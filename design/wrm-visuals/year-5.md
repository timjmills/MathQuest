# WRM visual catalogue — Year 5

Phase 3 of `design/WRM_ALIGNMENT_PLAN.md`: every representation White Rose Maths draws in the Year 5
small-step materials, how WRM draws it, and whether our kit can already draw it in our own black-and-white
Andika style. **Descriptions only** — no WRM artwork, wording or layouts are copied into the repo; a
match means "our own drawing carries the same mathematical grammar", never "looks like WRM". Year 5 = US
**Grade 4** (school rule in `data/curriculum/wrm-steps.json`).

- Source: `data/curriculum/wrm-steps.json` (branch `claude/sweet-newton-c8wrv1`), Year 5: 15 blocks,
  **136 small steps**, every step's `drive.lesson` (the 3-page small-step PDF) and `drive.guide` (Teaching
  Guide). Viewed 2026-09-25.
- For **every** step: the Teaching Guide's "Key model" and "Avoid" lines (all 136 guides are Guide D with a
  "Key model:" line — the keyword fallback never fired) **and** pages 2 (*Key learning*) and 3 (*Reasoning
  and problem solving*) of the small-step PDF, rendered at 52 dpi, tiled three steps per contact sheet and
  looked at. Worksheets and slide decks were not opened.
- Coverage honesty: both image pages were viewed for all 136 steps (46 contact sheets); no download was
  lost (see method notes).

## Legend

| Status | Meaning |
|---|---|
| **MATCH** | An existing kit template / option (or a legacy skill visual that is already B&W-compliant) draws the representation with the same structure. Cite = what to use. |
| **PARTIAL** | We draw the representation, but something WRM relies on differs (task, number range, orientation, what is blank, labelling) — or the drawing exists only as a **support pane** that is not yet wired as a skill option, or only as a **legacy** (coloured, non-kit) visual. What differs is stated. |
| **GAP** | Nothing draws it. A proposal is given: new option on skill X, or new template / skill. |

Citations: `template:<id>` = `js/modules/sheet/cells/<file>.js` registered cell; `pane:<id>` = support
pane in `js/modules/sheet/cells/panes/*.js` (registry `panes/index.js`); `category:skill` = skill key;
option ids are from `js/modules/skill-options.js`. `R`-numbers are the de-duplicated ids of the Year 3
table (R01–R84) and the Year 2 table (R85–R101); `Y1 R..` = a row of the Year 1 table. Representations
**new in Year 5** (in none of the Year 1, 2 or 3 tables) are numbered **N5-01 … N5-30** — a lane-local
prefix so that parallel year lanes cannot collide on R-numbers; the merge assigns final ids.

**Headline finding.** Year 5 is a **chart-and-bar year**. Almost every number step puts a **place-value
chart with counters beside the written method** (column +/−, short ×, bus-stop ÷, decimal +/−), every
fraction step is taught on **stacked bar models** (equal length, re-partitioned, crossed out, extended past
one whole), and decimals and percentages are taught on the **hundred square as one whole**. Three things in
the kit lift Year 5 rows: `template:stack` (whole-number column methods with regroup rows), the B&W
`frac-model.js` behind the `model` option on seven fraction skills, and the Grade-4 rounding family
(`number_sense:round_nl_*`, `pane:round-line`, `round-chart`, `round-pv`) plus `placevalue:place_value_10x`
with its `support: 'shift'` chart (incl. `decimals`). The real GAPs are (1) **decimal place value** — our pv
drawings stop at the ones (`pv.js` `LETTER` = O … M), so there is no O·Tth·Hth·Thth chart, no 0.1 / 0.01 /
0.001 counter, and `stack` hands decimals back to the legacy path; (2) **the PV chart beside the method**
(R31) and **plain-dot charts** (R05), which Year 5 uses in ≈ 30 steps; (3) **new Year 5 families**: Roman
numerals, the Gattegno chart, period-grouped (Thousands | Ones) charts, long multiplication with partial
rows, short division, the protractor, angles at a point / on a line, coordinates with translation and
reflection, line graphs and timetables, vertical number lines through zero, conversion ladders and
isometric cube solids.

---

## Block 1 — Autumn 1 · Place value (14 steps)

| Step | WRM representations (how drawn) | Match |
|---|---|---|
| 1 Roman numerals to 1,000 | **Roman part-whole**: a whole on top split into Roman parts (e.g. a number → D · L · XX · IX), parts sometimes given as a word ("five hundred"); **match Roman cards to numerals** (two columns of cards); a date written in Roman numerals; film-credit years; Roman calculations; a **Roman sequence track** (+CXX, blanks). Guide: symbol→value table + partition; Roman numerals have no zero and no column value. | **GAP** → new skill `placevalue:roman_numerals` (N5-01; tasks `to-roman` / `from-roman` / `match` / `sequence`; `range` 100 / 1,000; `support: 'table'` prints the symbol→value key as a structural scaffold). Part-whole of Roman parts: `template:bond` with text values (PARTIAL — bond holds numerals only; `values: 'text'`). Track: `template:seqstrip` with text tiles (PARTIAL). |
| 2 Numbers to 10,000 | **Base-10 incl. the thousand cube** (big cube, flats, rods, cubes) to read; PV counters loose (1,000 / 100 / 10 / 1); part-whole (whole blank, parts 2,346 and 5,000); **Th\|H\|T\|O chart with plain unlabelled dots**; match three representations (base-10, counters, part-whole) to three near numbers (4,005 / 4,500 / 4,050); add / remove a counter in the chart; **table "Add 10 \| Add 100 \| Add 1,000"** with blanks in any column. | Counters: MATCH `template:pv` kind `disks` (`placevalue:place_value_disks`, band to 9,999). Base-10 with thousands: PARTIAL R01 (`pane:base10` to 99; no cube-of-a-thousand drawing). Plain dots: **GAP** R05. Add-10/100/1,000 table: GAP R35 (`layout: 'table'` on `placevalue:more_less_100`, steps 10/100/1000 exist). Match-three: GAP Y1 R48 ("which pictures show n"). |
| 3 Numbers to 100,000 | **TTh\|Th\|H\|T\|O chart with value counters**; missing-number partition equations (59,000 = 50,000 + __); **"complete the grid"** — one number shown four ways in a 2 × 2 frame (counters \| part-whole \| bar model \| PV chart); chart with plain dots, a counter moved / added; count down in 10,000s; **number line 30,000–40,000** (10 intervals) with arrows above and below; word cards to build numbers; digit cards (list all 5-digit numbers). | Chart with counters: PARTIAL R04 (`placevalue:pv_disks_build` band ≤ 999, `place_value_disks` ≤ 9,999 → add 99,999 / 999,999 bands; the drawing already has TTh/HTh letters). Equations: MATCH `placevalue:expand` (`form`, bands to 999,999). Four-way grid: **GAP** N5-25. Number line: PARTIAL R17. Digit cards: PARTIAL R81. |
| 4 Numbers to 1,000,000 | **6-column chart HTh…O with plain dots**, and the **same chart with two-tier heads "Thousands (H T O) \| Ones (H T O)"**; numbers to make on an empty two-tier chart; value of a digit; count in 100,000s; **bar model** 1,000,000 over two parts and **part-whole** 1,000,000 → two parts (partition into multiples of 100,000); digit cards greatest / smallest. Guide: commas in groups of three. | Value / identify: MATCH `placevalue:value`, `identify` (bands to 999,999; `_pvDigitSupport` `support: 'chart'`). Two-tier period heads: **GAP** N5-02 (`heads: 'periods'` on the pv chart drawing and `pv_digit_drag`). Bar / part-whole: PARTIAL R25 / MATCH R11. |
| 5 Read and write numbers to 1,000,000 | **Part-whole split at the comma** (145,362 → 145,000 and 362) beside the number in words; two-tier Thousands \| Ones chart with dots → words; numeral cards → words; word cards → numerals ("half a million"). | Words: MATCH `placevalue:number_word_names` (band 999,999). Split-at-comma bond: MATCH `template:bond` (numbers). Two-tier chart: GAP N5-02. |
| 6 Powers of 10 | Empty two-tier chart to make 425 then 4,250 (column shift); **Gattegno chart** (rows 1–9, 10–90 … 100,000–900,000; cells ringed to show a number); "how many tens in 210?"; km ↔ m, m ↔ mm sentences; hectares (10,000 m²). Guide: no index notation; never "add a zero". | ×/÷ 10, 100, 1,000: MATCH `placevalue:place_value_10x` (`support: 'shift'` — digits move across the places). Gattegno: **GAP** N5-04 → `template:gattegno`. "Tens in 210": MATCH `placevalue:unit_form` (`rename: 'more'`). Unit sentences: PARTIAL `measurement:length_metric`. |
| 7 10/100/1,000/10,000/100,000 more or less | **Gattegno chart** with a number ringed → 10 / 100 / 10,000 more or less; TTh chart with plain dots → 100 more / 10 less / 1,000 less; **number tracks** (6 boxes, steps of 10 / 200 / −400, blanks); count on in 1,000s / 10,000s / 100,000s; spot the mistake in a card sequence. | `placevalue:more_less_100` has steps 10 / 100 / 1,000 only → PARTIAL (add 10,000 and 100,000 steps and a 999,999 band). Dots: GAP R05. Gattegno: GAP N5-04. Tracks: MATCH R22 `template:count-row` (`patterns:count_by_step_up/down`, `count_by_powers_of_10`). |
| 8 Partition numbers to 1,000,000 | Four-blank partition equations; **PV counters in a row that must be exchanged** to make non-standard partitions (32,426 = 20,000 + __ + 400 + …); a jumbled expanded sum with an error; **three-part part-whole** (whole on top, three parts, some given); Roman partition (MMDXL = M + M + D + …); odd-one-out cards (680,000 / 680 thousands / 68 ten-thousands / 680 hundreds); **counters hidden under a splat** (total known, find the hidden ones). | Equations incl. non-standard: MATCH `placevalue:expand` (`rename`) / `unit_form` (`rename: 'more'`). 3-part cherry: **GAP** R13 (`parts: 2…5` on `template:bond`). Hidden counters: GAP R87. Roman: GAP N5-01. |
| 9 Number line to 1,000,000 | End-labelled lines 0–100 / 0–1,000 / 0–10,000 / 0–100,000 (10 intervals) to label ("what is the same?"); arrows to read; lines with **100 minor ticks** and labels every 10 (thousands); label the ends from interior labels; line with **no interior ticks** — draw arrows (exact / approximate); estimate one number on four lines with different ends; digit cards into "__,000" labels. Guide: find the interval value first. | PARTIAL R17 — `number_sense:place_on_number_line` (`template:pv` kind `line-mark`) marks between two multiples only. **GAP** for label / read-arrow / place / estimate at 5–6 digits → the proposed `number_sense:read_number_line` (Y3 R17/R18) with `start`, `interval` up to 100,000 and `ticks: 'all' \| 'ends' \| 'ends+mid' \| 'none'`. |
| 10 Compare and order numbers to 100,000 | Pairs (63 / 68, 63,000 / 68,000); scattered PV counters vs a TTh chart of dots (which is greater?); **compare circles**; a 0–100,000 line to order; six counters on an empty chart to make 5-digit numbers; mixed forms to order (numeral, **Roman**, words, "5,000 more than …"); digit cards and clues. Guide: compare from the greatest place. | MATCH `template:pv` kind `compare` / `order` (`placevalue:compare`, `order_*`, bands to 999,999). Pictures either side: PARTIAL R34. Mixed forms: PARTIAL `number_word_names`; Roman GAP N5-01. |
| 11 Compare and order numbers to 1,000,000 | Pairs in growing magnitude; compare circles; order with a 0–1,000,000 line (ends + midpoint labelled); **table town \| population** to order; **0–1,000,000 line with no interior ticks** to estimate word-numbers; compare circles between expressions and unit forms (600,000 + 80,000 ○ 618,000; 20 ten-thousands ○ 200 thousands). | MATCH `placevalue:compare` / `order_*`. Table: GAP R58. Estimate line: PARTIAL R18. Expressions: PARTIAL `placevalue:unit_form` (compare task absent). |
| 12 Round to the nearest 10, 100 or 1,000 | **Bounding-multiple line** 720–730 (mark 728, round to 10), then 700–800 (round to 100); "between which two multiples of 1,000?"; the halfway number; word problems (attendances, running distances); cards that round to 4,600. | MATCH N5-30: `number_sense:rounding_visual` / `nearest_10/100/1000` (`line: 'mark'` with `pane:round-line`, `midLabel`), `round_nl_thousands`, `between_tens`. Inverse rounding ("what could it be?"): GAP (task option). |
| 13 Round within 100,000 | 40,000–50,000 line with **only a midpoint tick** (what is halfway? draw an arrow, round); 86 / 174 vs 86,000 / 174,000; cards that round to 10,000; estimating a sum by rounding; smallest number a headline could mean. | MATCH N5-30 `number_sense:nearest_10000`, `round_nl_ten_thousands`, `round_sort_10000`, `estimate_sum`. Inverse rounding: GAP (as S12). |
| 14 Round within 1,000,000 | 0–1,000,000 line (10 intervals) to complete; between which multiples of 100,000; **table statue \| mass (kg)** to round; round a distance to 1,000 / 10,000 / 100,000; greatest / smallest integer that rounds to a value; batches of 10,000 (round up in context). | MATCH N5-30 `nearest_100000`, `round_nl_hundred_thousands`, `rounding_table` (a rounding table exists). Data table: GAP R58. |

## Block 2 — Autumn 2 · Addition and subtraction (8 steps)

| Step | WRM representations (how drawn) | Match |
|---|---|---|
| 1 Mental strategies | Related-fact chains (8 + 4 → 8,000 + 4,000); **TTh chart of plain dots** as a subtraction support; a 100s line 800–1,300 with an arrow (1,050 ± 100); **compensation on a jump line** (+100 arc above, small −1 back hop: add 99); cards ± 999 / 990; "same difference" (1,000 − 372 = 999 − 371). Guide: line for jumps & compensation; partition jottings. | `number_sense:compensation`, `addition:add_sub_10s/100s` exist (equations). Compensation line: PARTIAL R20 (`pane:openline` not wired; no over-and-back hop pair) → `hops: 'compensate'` on the open line. Dots: GAP R05. Same-difference: GAP (low; constant-difference lines, see S8). |
| 2 Add whole numbers with more than four digits | **Column addition on squared paper** (2–5 digits, carry written under the line); **PV chart Th\|H\|T\|O with counters beside the column**, the exchange shown as a ring of ten ones and an arrow to one ten; missing-digit column (boxes in any row); misaligned-digit error. Guide: carry under the line. | Column: MATCH R30 `template:stack` (`regroup: 'add'`, `answer: 'boxes'`) on `addition:add_10k_regroup` / `add_10k_mixed` — bands stop at 10,000, Y5 adds 5- and 6-digit numbers → PARTIAL (add `100k` / `1m` bands); carry written *below* the line vs our regroup row above (`regroup: 'below'` option). Chart beside: **GAP** R31 `pane:pv-exchange`. Missing digits: MATCH R33. |
| 3 Subtract whole numbers with more than four digits | Column subtraction on squared paper (2–5 digits); **TTh chart of counters beside the column**, unbundling one from the left; word problems (populations); missing digits; "smaller from larger" error. | Column: MATCH R30 `template:stack` (`regroup: 'sub'` headroom) on `subtraction:sub_10k_regroup` — 5-digit band PARTIAL as S2. Chart beside: GAP R31. |
| 4 Round to check answers | Rounding frames ("6,789 rounded to the nearest 1,000 is __; estimate __ + __ = __"); cards to round to 100,000; **table month \| tickets**; **price tags on pictured items** (budget check); estimates made at different powers of ten compared. | MATCH `number_sense:estimate_sum` / `estimate_diff` / `estimate_sums_diffs`. Table: GAP R58. Price tags: PARTIAL R69. |
| 5 Inverse operations | **Bar model** (whole over two parts) → two additions and two subtractions; an addition → which subtraction checks it (cards); bar with a blank whole; "think of a number" → which calculation; **number pyramid** (each brick the sum of the two below, blanks). Guide: part-whole / bar → fact family. | Fact family from a bar: GAP R25 (`model: 'bar'` on `addition:add_sub_fact_family`; the family template uses a bond). Missing numbers: MATCH `algebra:solve_unknown` / `template:equation`. Pyramid: **GAP** N5-11 `template:pyramid`. |
| 6 Multi-step addition and subtraction problems | Word problems; **comparison bars with a brace for the total and the difference marked on the longer bar** (sum-and-difference); price tags on appliances with a budget; pole partly under water; ages puzzle. Guide: bars reveal the hidden step. | PARTIAL R26 `template:word-work` `wpBar` (comparison look; no total brace) → `brace: 'total'`. Price tags: PARTIAL R69. |
| 7 Compare calculations | **Two bar models stacked, aligned at the left** (983 \| 410 vs 983 \| 510); compare circles between expressions (47 + 28 ○ 37 + 28); order calculation cards; compensation "same answer" (564 + 478 = 563 + 479); related calculations from one known fact. Guide: structure, not arithmetic. | Compare circle between expressions: MATCH R34 (`addition:equal_sign` / `comparison_word`-style frame). Stacked bars: PARTIAL R26. |
| 8 Find missing numbers | Missing numbers in any position; **bar pairs showing compensation** (97 \| 54 over 100 \| □); **two parallel number lines with the same hop above both numbers** (constant difference: 85 − 27 = 88 − □, dashed ties between the lines); bar from a first calculation to find a second; match same-result cards; inequality with a box (327 + □ < 700); **a shape as the unknown** in an inequality; missing digits __3_ + __3 = 300. Guide: bar for totals, line for differences. | Missing numbers: MATCH `template:equation` / `algebra:solve_unknown`. Constant-difference lines: **GAP** (R20 extension `lines: 2`, low). Shape unknown: GAP R92. Inequality box: PARTIAL `algebra` inequality skills. |

## Block 3 — Autumn 3 · Multiplication and division A (10 steps)

| Step | WRM representations (how drawn) | Match |
|---|---|---|
| 1 Multiples | **Rows of counters** grouped in 5s (5, 10, 15 as growing arrays); cards: which are multiples of 5 / 10 / 2 (sort); lists of multiples of 4 and 8; divisibility rules (halve and get even; digit sums of 3 and 9); shade multiples on a hundred square. Guide: counter array / times list. | MATCH `number_theory:multiples`, `divisibility_sort` (legacy drag, B&W OK). Arrays: MATCH R38 `template:arrays`. Hundred square shade: PARTIAL R23. |
| 2 Common multiples | **Numbered hundred square** — shade multiples of 5, circle multiples of 3 → common; bell / whistle every 6 / 8 seconds; **2 × 2 Carroll table** (multiple of 7 / not × multiple of 4 / not) for 1–30; **Venn diagram** (multiples of 6 \| multiples of 9) with number cards; always / sometimes / never. Guide: two-row lists, hundred grid, Venn; no "LCM" term. | `number_theory:lcm` (equation). Hundred square: PARTIAL R23 (`task: 'shade'` + `task: 'circle'` on the whole chart). Carroll: **GAP** R44. Venn: **GAP** R43 → `template:venn` + `number_theory:common_multiples`. |
| 3 Factors | **4 × 5 counter array** → factors of 20 (other arrays); cards: which are factors of 60; a **systematic factor-pair list** (1 × 24, 2 × 12 … 5 × ✗); cards to test 5 or 3 as a factor; factor-fact equations (2 × □ = 14 so 6 × 14 = 6 × 2 × □); **two-row factor table** (1 2 3 4 5 6 over 36 18 12 9 ✗ 6). Guide: array → factor-pair "bug". | MATCH `number_theory:factors_identify`; factor pairs PARTIAL N5-12 `number_theory:factor_tchart_*` / `factor_links_*` (legacy drag, B&W). Arrays: MATCH R38. |
| 4 Common factors | Counter arrays in a box (12 and 15 in one row → 1 is common; 2 rows → 2 is not); **Venn "factors of 30 \| factors of 40"** with number cards; a blank Venn for 20 / 24; common factors of pairs (cards); true / false cards. Guide: list both, then Venn. | `number_theory:gcf_easy` (equation). Venn: GAP R43 → `number_theory:common_factors` on `template:venn`. |
| 5 Prime numbers | Prime cards (make their factors with counters); prime / composite cards; **sieve on a hundred square** (shade 1, then multiples of 2, 3 … 7); **2 × 2 Carroll table prime / composite × even / odd**; prime factors of 24; **table "ones digit 1 \| 3 \| 7 \| 9"** columns to sort 2-digit primes. Guide: count the factors; 1 is not prime. | MATCH `number_theory:prime_composite`. Sieve: PARTIAL R23 (`pane:hundreds` whole-chart shade task). Carroll: GAP R44. Column sort: PARTIAL R59. |
| 6 Square numbers | **Counters in an n × n square array** (3 × 3 = 9; 6 × 6); cards: which are square; square arrays for 7 × 7 … 10 × 10; **notation 5²**; digit cards to make a square / prime / multiple; sum of two primes = a square; a hollow 3 × 3 ring of 8 counters (not square). | Arrays: MATCH R38 `template:arrays` (square case). Notation / square-number skill: **GAP** → `number_theory:square_numbers` (tasks array / notation / identify; `n²` typeset per TY rules). |
| 7 Cube numbers | **3 × 3 × 3 cube of linking cubes** (27) in a 3-D drawing; **table size of cube \| calculation \| number of cubes** (1³ … 6³, blanks); column multiplication 49 × 7 on squared paper; compare circles 5² ○ 4³; letter-card puzzle. Guide: build a solid cube → n³; n³ ≠ n × 3. | Cube drawing: **GAP** N5-13 (`template:cube-solid`). Table: GAP R58. Skill: GAP `number_theory:cube_numbers`. Column: MATCH R30 `stack` (`multiplication:multiply` `tiles: 21`). |
| 8 Multiply by 10, 100 and 1,000 | **HTh…O chart with dots** for 234 — where do the counters move ×10 / ×100; missing products (156 × 100 = __); ladders 4 × 10 / 100 / 1,000; compare circles 71 × 1,000 ○ 71 × 100; 6 × 10² notation; **Gattegno chart** with a cell ringed → 1,000 times; money stories. Guide: counters slide left; never "add a zero". | MATCH `placevalue:place_value_10x` (`op`, `power` 10/100/1000, `support: 'shift'`). Dots sliding: GAP R05. Gattegno: GAP N5-04. Compare: MATCH R34. |
| 9 Divide by 10, 100 and 1,000 | HTh chart of dots → ÷10 / ÷100; cards; **digit PV charts** (HTh … O with digits 6 4 0 0 0) beside ÷10 / ÷100 / ÷1,000 ladders; missing numbers; "÷100 = ÷10 twice" (always true?); one ten-thousandth of a sum of money. | MATCH `placevalue:place_value_10x` (`op: 'divide'`) + R06 digit chart. Dots: GAP R05. |
| 10 Multiples of 10, 100 and 1,000 | **Two worked methods in boxes side by side** (24 × 10 × 2 vs 24 × 2 × 10); cards 21 × 30, 43 × 20; an **arrow chain** (7,200 → ÷100 → 72 → ÷2 → 36: stacked boxes with curved labelled arrows); related facts from 36 × 5 = 180; clue cards ("a multiple of 30 between 100 and 200"); an error in a worked division. Guide: factor split + known fact. | MATCH `multiplication:mult_zeros` (multiply by 10, 100 and multiples of ten). Arrow chain: PARTIAL R36 (`template:function-table` → chained look). Two methods: PARTIAL N5-26 (error-analysis / reasoning roles). |

## Block 4 — Autumn 4 · Fractions A (17 steps)

The B&W fraction model `frac-model.js` (area / bar / circle / 0–1 line) is offered through the `model`
option on `fractions:identify`, `write_fraction`, `shade_fraction`, `compare`, `equiv_frac_visual`,
`fraction_operations:add_fractions_like`, `sub_fractions_like`. It draws **one** model of one fraction:
Year 5 needs **two or more stacked, equal-length bars**, bars that **run past one whole**, **mixed-number
bars** (whole bars + a part bar), **crossed-out parts**, and bars cut into **two different partitions**
(halves re-split into eighths). Those are the recurring PARTIALs below (R54 options `stack`, `wholes`,
`cross`, `split`).

| Step | WRM representations (how drawn) | Match |
|---|---|---|
| 1 Find fractions equivalent to a unit fraction | **Two same-size paper strips**, one halved, one cut into eighths (stacked bars); area rectangles cut in vertical strips / grids and shaded; **two stacked 0–1 number lines** (5 and 10 intervals) with arrows at equal points; **multiplier arrows** (×4 arcs over the numerators and under the denominators across the = sign) beside a grid; missing-box equivalents (1/2 = □/12); true / false cards; a chain 1/6 = □/12 = 3/□. Guide: strip re-partitioned, then a line. | Equivalents: MATCH `fractions:equiv_frac_visual` (`model`), `equiv_frac_nv`, `select_equiv_frac`. Stacked strips: PARTIAL R54 (`stack: 2`). Stacked lines: PARTIAL R56. Multiplier arrows: **GAP** N5-08 (mark pane `scale-arrows`). |
| 2 … equivalent to a non-unit fraction | Folded strips (3/4 = 6/8); **a bar of sixths over a bar of twelfths**, every cell labelled with its unit fraction (a two-row fraction wall); stacked number lines (fifths / tenths); area grid + ×4 arrows; rule-check cards. | As S1: PARTIAL R54 (`stack`, `labels: 'unit'`), R56; GAP N5-08. |
| 3 Recognise equivalent fractions | ×3 / ×6 and ÷4 arrow diagrams with area rectangles; cards "show 1/3 = 5/15"; cards equivalent to 1/5 or 3/8; sort six fractions into pairs; digit cards 4/6 = □/□; a chain with ÷ arrows. Guide: fraction wall rows. | MATCH `select_equiv_frac`, `equivalent`, `simplify`. Arrows: GAP N5-08. |
| 4 Convert improper fractions to mixed numbers | Bar of sixths = 1; two bars of fifths = 2 = 10/5; **linking-cube towers of 5** (14/5 → 2 full towers + 4); **stacked full bars + a part bar with a brace** (27/8 = 3 3/8); label 7/5 and 13/5 on a **0–3 line in fifths**; two conversions compared; the division-with-remainder frame (23 ÷ 4 = __ r __). Guide: group into wholes, then 32 ÷ 7 = 4 r 4. | MATCH `fractions:improper_mixed`; `mixed_improper_visual` (legacy pizza circles, coloured). Whole bars + part bar: PARTIAL R54 (`wholes`). Cube towers: GAP R41. Line past 1: PARTIAL R55 (`fractions:mixed_nl_drag`, `ticks`). |
| 5 Convert mixed numbers to improper fractions | **Circles in quarters** (2 full + 3/4); bars of fifths (1 full + 3/5); **cube towers** (3 of 5 + 2); a structured working frame (__ wholes + __ fifths = __ fifths); 0–3 line in thirds (1 2/3, 2 1/3); digit boxes (2 □/8 = □/8, many ways). | As S4: MATCH `improper_mixed`; circles PARTIAL (`frac-model` circle, one whole only → `wholes`); R41, R55. |
| 6 Compare fractions less than 1 | **Two equal-length bars stacked** (2/5 vs 2/3) with compare circles; same-numerator / same-denominator pairs; **bars re-split to a common denominator** (quarters → eighths); a chocolate-bar story; compare circles; benchmark ½; distance from 0 vs from 1 on a line. Guide: equal-length bars → common denominator. | MATCH `fractions:compare` (`model`), `compare_frac_lcd`, `benchmark_fractions`. Stacked bars: PARTIAL R54 (`stack`, `wholeMm` already equalises widths). |
| 7 Order fractions less than 1 | **0–1 line in twelfths** to label; convert to twelfths then order; ascending sets (same numerator / same denominator); order with <; digit boxes 3/8 < □/□ < 3/4. | MATCH `fractions:order_fractions`, `order_frac_numline`, `fraction_nl_drag` (`ticks`). |
| 8 Compare and order fractions greater than 1 | Compare circles between mixed numbers; order mixed numbers; **rows of whole bars + a part bar** to explain 2 1/3 < 2 1/2; **two whole bars each** (7/6 vs 5/3) with "__ > __" frames; common denominators; 2 3/4 < □ < 10/3. | PARTIAL `fractions:compare` (mixed numbers?) → R54 `wholes`. `mixed_nl_drag` MATCH for the line form. |
| 9 Add and subtract fractions with the same denominator | Bar in fifths, 1/5 + 2/5 shaded in two tones; **9/5 − 6/5 over two whole bars**; two stacked bars 3/5 + 4/5 = 7/5 = 1 2/5; missing-number sums; a flag of coloured parts; a pictured chocolate bar. Guide: shade to add, cross out to subtract. | MATCH `fraction_operations:add_fractions_like` / `sub_fractions_like` (`model`). Two tones: our rule is one grey + outline (INK-3) — use hatched vs grey. Past one whole: PARTIAL R54 `wholes`. Cross out: PARTIAL R32. |
| 10 Add fractions within 1 | **Area rectangle split in halves one way and eighths the other** (1/2 + 1/8); a bar in eighths with quarters shaded; which common denominator (cards); **a bar of 20ths with brackets below for 2/5, 1/10, 3/20**; missing numbers; the add-tops-and-bottoms error. Guide: fraction wall / stacked bars → common denominator. | MATCH `add_frac_unlike` (legacy visual) / `add_frac_unlike_nv`. Two-way area split: PARTIAL R53 (`frac-model` area `split: [2, 8]`). Bracketed bar: PARTIAL R54 (`brackets`). |
| 11 Add fractions with total greater than 1 | **Two stacked bars in eighths** (3/4 + 5/8 = 11/8 = 1 3/8); a bar of 12ths holding thirds, sixths and twelfths; blank bars to use; two 4 × 4 / 3 × 4 grids (area) to find combinations; clue puzzle. Guide: the bar extends past one whole. | `add_frac_unlike` (legacy). Bars: PARTIAL R54 (`stack`, `wholes`). |
| 12 Add to a mixed number | **Partition flow** (a mixed number split into whole + fraction, the fractions added, recombined — drawn as a vertical branching tree); the same for whole + mixed; an error (4 3/8 + 1/8 = 4 4/8 left unsimplified); an equivalent-fraction working chain. Guide: part-whole & bar; number line. | MATCH `fraction_operations:add_mixed_like` / `add_mixed_unlike` (legacy visuals). Partition tree: **GAP** N5-27 (low). |
| 13 Add two mixed numbers | Bars for 2 2/5 + 3 1/5; wholes-first method; equivalent-fraction chain; improper-fraction method; a better-answer check; water drunk (litres); **number tracks of mixed numbers** (blanks, find the step); 3 2/5 + □ = 81/10. | MATCH `add_mixed_like` / `add_mixed_unlike`. Fraction track: PARTIAL R22 (`template:count-row` with fraction values; cf. Y2 R95). |
| 14 Subtract fractions | **Area rectangle of fifteenths with hatched crossed-out parts** (1/3 − 1/15); **0–1 line in ninths above a line in thirds** with back hops (2/3 − 2/9); two blank stacked bars (3/4 vs 5/12) to draw; difference cards; the **midpoint of a fraction line** (ends given, box at the middle). | MATCH `sub_frac_unlike` (legacy) / `_nv`. Double line + hops: PARTIAL R56 + R20. Hatched cross-out: PARTIAL R32. Midpoint: GAP (R18 `task: 'midpoint'`, low). |
| 15 Subtract from a mixed number | **Whole squares (quartered) crossed with a big X** plus a part (3 5/8 − 2); "what calculation does this show?"; a whole + eighths bar with hatched cross-outs; **counting-on number line for a difference** (a +1 hop and a +1/10 hop); an error; a rectangle with crossed parts. | MATCH `sub_mixed_like` / `sub_mixed_unlike`. Cross-out: PARTIAL R32. Difference line: PARTIAL R20. |
| 16 … breaking the whole | **Two whole bars, one broken into thirds**, crosses on the parts (2 1/3 − 2/3); two improper-fraction workings side by side; dashed-split bar with crossed parts; flexible partitioning (6 4/9 − 2/3 = 5 + 1 4/9 − 2/3); digit cards into boxes; **0–4 line in fifths** to show 2 3/5 − 4/5; three partitions compared. | MATCH `sub_mixed_like`/`unlike`. Break-a-whole bar: PARTIAL R54 (`wholes`, `cross`, `split` one whole). Two methods: PARTIAL N5-26. Line: PARTIAL R55. |
| 17 Subtract two mixed numbers | 6 4/5 − 2 3/5 as **whole bars and a part bar, wholes and parts crossed out**; 3 3/4 − 1 1/8 bars; breaking a whole; **"Method 1 \| Method 2" table** of two workings; ribbon (cm) and dog-food (kg) stories. | MATCH `sub_mixed_unlike`. Bars: PARTIAL R54. Two methods: PARTIAL N5-26. |

## Block 5 — Spring 1 · Multiplication and division B (11 steps)

| Step | WRM representations (how drawn) | Match |
|---|---|---|
| 1 Multiply up to a 4-digit number by a 1-digit number | **PV chart (Th\|H\|T\|O) with the number repeated in n identical rows of counters** (2,103 × 3) + sentence frames ("__ ones altogether …"); a chart of 4 rows beside a **column grid** (weekly pay × 4); column multiplication on squared paper with the carry row below; the no-exchange error (1,432 × 4 = 416,128); missing digits with clues. Guide: chart beside the column; carry below the line. | Column: MATCH R30 `template:stack` (`multiplication:multiply` `tiles: 21 / 31`; **no 4-digit × 1-digit tile** → PARTIAL, add `tiles: 41`). Repeated-rows chart: **GAP** R31 (`pane:pv-exchange` `rows: n`). Missing digits: MATCH `multiplication:mult_missing_digit`. |
| 2 Multiply a 2-digit number by a 2-digit number (area model) | **Area model 2 × 2 with base-10 drawn in every cell** (flats, rods, cubes: 23 × 13); **area model filled with PV counters**; area model with numbers (40 \| 4 by 30 \| 2) + partial products summed; missing side (42 × □ = 504). Guide: base 10 → counters → numbers. | Numbers: PARTIAL R16 — `multiplication:area_model_mult_hard` (2 × 2 / 2 × 3, **legacy** input grid) and kit `template:area-model` (1-digit multiplier only) → kit `rows: [tens, ones]`. Pictures inside cells: **GAP** (`fill: 'base10' \| 'disks'`). |
| 3 Multiply a 2-digit number by a 2-digit number | **Area grid beside the column method** (same product two ways); **long multiplication with partial-product rows annotated "(32 × 3)", "(32 × 10)"**, carries, a blank "(__ × __)" to fill; the forgotten placeholder zero error. | Long multiplication: PARTIAL N5-09 — `multiplication:multiply` `tiles: 22` prints the legacy `column-mult` with partial rows; the kit `stack` has one product row → `partials: true` (+ row labels as a hint scaffold). Placeholder zero: MATCH `multiplication:mult_placeholder_zero`. |
| 4 Multiply a 3-digit number by a 2-digit number | Long multiplication (3-digit × 2-digit) with partial-row labels; estimate first; playground / rugby-pitch areas; plane-ticket cost; 22 × 111 pattern; two workings with errors. | PARTIAL N5-09 (`mult_placeholder_zero` `tiles: 32` exists; no full 3 × 2 tile on `multiply`). Estimate: MATCH `number_sense:estimate_products`. |
| 5 Multiply a 4-digit number by a 2-digit number | Long multiplication 4-digit × 2-digit with blank partial labels; estimates; compare circles between products (4,458 × 56 ○ 4,523 × 54); **paint splats hiding digits**; digit cards into a **"□□□□ × □□" box template** (greatest product). | PARTIAL N5-09 (add `tiles: 42`). Hidden digits: PARTIAL `mult_missing_digit` / R87. Box template: PARTIAL R81. |
| 6 Solve problems with multiplication | Two methods (long multiplication vs × 100 − 1 lot) in speech; compare circles without calculating (2,470 × 83 ○ 247 × 830); factor-pair strategies (× 3 then × 8 = × 24); a pictured price board; digit-card box template. Guide: bar model + adjust from a known fact. | Word problems: MATCH `multiplication:mult_word_problems`. Compensation: PARTIAL `number_sense:compensation` (add/sub only). Two methods: PARTIAL N5-26. |
| 7 Short division | **Part-whole 48 → 40 and 8 with a ÷4 arrow under each part** beside **short division (bus stop) on squared paper**; T\|O chart with counters grouped in threes + sentence frames; H\|T\|O chart of counters **ringed in groups** beside an empty bus stop; **an exchange arrow (one hundred → ten tens)** for 605 ÷ 5; missing digits in the bus stop; the missing-placeholder error. Guide: counters on H·T·O beside the algorithm. | Short division: PARTIAL N5-10 — `template:division` draws the long-division bracket with work rows (`division:divide` `tiles`, `long_div_2digit`) → `form: 'short'` (quotient boxes, carried digits as small raised boxes, no work rows). Split-and-divide part-whole: GAP R15 (`template:split-op`). Chart beside: GAP R31. Zero in quotient: MATCH `division:div_zero_in_quotient`. |
| 8 Divide a 4-digit number by a 1-digit number | Th\|H\|T\|O chart of counters + empty bus stop; **chart with groups ringed and an exchange arrow** beside a bus stop with carried digits; bus-stop templates; word problems (boxes, charities, miles, bags); missing digits; compare circles between divisions. | PARTIAL N5-10 (`division:divide` 4-digit ÷ 1-digit tile?) + GAP R31. Word problems: MATCH `division:div_word_problems`. |
| 9 Divide with remainders | Sharing with a remainder; the frame "__ ÷ __ = __ r __"; **H\|T\|O counters with exchange arrows beside a bus stop with "r2"**; muffins into boxes; patterns 36 ÷ 3 … 39 ÷ 3; missing digits with a remainder. | MATCH `division:div_remainders` (legacy visual), `remainder_interpret`, `remainder_contexts`, `remainder_too_big`; bus stop with r: PARTIAL N5-10. |
| 10 Efficient division | 300 ÷ 1 / 10 / 100 pattern; **a 2 × 4 counter array with dashed split lines** (8 ÷ 4 = 8 ÷ 2 ÷ 2); a factor method in a box (810 ÷ 6 = 810 ÷ 2 ÷ 3); **four methods for one division**: part-whole partitioning with ÷ arrows, short division, halve-and-halve-again, and **groups of 4 as two arcs along an open number line**; divisibility claims. | Split array: PARTIAL R38 (`template:arrays` split line). Part-whole ÷: GAP R15. Open-line groups: PARTIAL R19 (`template:hop-line` `nl_div` hops of one size; two unequal "4 × __" arcs → `hops: 'chunk'`). Four methods: PARTIAL N5-26. |
| 11 Solve problems with multiplication and division | T\|O counters ringed in sixes with an exchange + bus stop "r5" + sentences (minibuses needed); **interpret the remainder** (tables needed = 45); party bags; hockey pitch area; a pictured train; car-park rows; packs of textbooks / beads (round up); crayon packs of 5 and 12. Guide: bar model + known → operation → answer planner. | MATCH `division:remainder_interpret`, `remainder_contexts`, `algebra:multi_step_word`. Planner: GAP (low; a three-box "Known \| Operation \| Answer" frame, reasoning page). |

## Block 6 — Spring 2 · Fractions B (7 steps)

| Step | WRM representations (how drawn) | Match |
|---|---|---|
| 1 Multiply a unit fraction by an integer | Whole-number × as repeated addition; unit fraction × n as repeated addition; **four stacked bars of sixths, one cell shaded in each, braced "4 sixths"**; **a bar of 7ths with five cells shaded and a brace "5/7"**; **a number line with labelled 1/5 blocks sitting on it** (0 … 1 … 2, 6/5); cards; the "multiply the denominator too" error. Guide: colour one fifth at a time and count. | MATCH `fraction_operations:mult_frac_whole` (legacy visual) / `_nv`. Stacked unit bars: PARTIAL R54 (`stack: n`). Blocks on a line: **GAP** (R55 `blocks: true` — fraction-length bars laid end to end on the line). |
| 2 Multiply a non-unit fraction by an integer | Repeated additions; **a bar of 7ths with the whole braced above and three 2/7 brackets below**; a product that runs **past one whole** (two bars); number line with 2/5 blocks; **four stacked bars of 14ths, 3 shaded in each, "?" brace**; patterns; digit cards □ × □/□ = □/□. | As S1: MATCH `mult_frac_whole`; PARTIAL R54 (`stack`, `wholes`, `brackets`). |
| 3 Multiply a mixed number by an integer | Repeated addition of a mixed number; **partition: a mixed number split into whole and fraction, each multiplied, recombined**; improper-fraction method; reasoning on simplifying; running miles; missing numbers. Guide: part-whole tree + bar of lots. | `mult_frac_whole` (whole × mixed? check range) → PARTIAL (mixed-number operand). Partition: GAP N5-27. |
| 4 Calculate a fraction of a quantity | 16 pictured cookies ringed into four groups; **a bar of 5 parts, each holding 4 counters** (20 shared); **a counter array 6 × 5 with dashed split lines** (5/6 of 30); **PV counters (tens and ones) placed in 4 bar cells** (1/4 of 84); a two-column sort "equal to 6 \| equal to 8". Guide: bar split into equal parts, filled with counters. | MATCH `fractions:fraction_of_set` / `_hard` (legacy visual). Bar with counters in the parts: **GAP** R27 (`fill: 'counters' \| 'disks'`). Split array: PARTIAL R38. Sort: PARTIAL R59. |
| 5 Fraction of an amount | **A bar with the whole written above and a numeral in every part** (35 → five 7s); a bar of 54 → six 9s, five shaded, "?" brace below; the rule (÷ denominator, × numerator); compare circles between fractions of amounts; patterns; draw bar models for amounts with units (kg, cm, ml); a 6 × 8 cm rectangle of lettered squares (area of each letter). Guide: numerals in the parts, no counters now. | `fractions:fraction_of_set_nv`, `conversions:percent_of_number`-style skills → PARTIAL (no "fraction of an amount" bar) → R27 `fill: 'numbers'` + new `fractions:fraction_of_amount`. |
| 6 Find the whole | Bar of 4 parts with 5 counters in the first (1/4 = 5 → 2/4, 3/4, whole); **a bar with "?" over the whole and one part labelled** (1/5 of ? = 6); 2/5 of ? = 6 (3 \| 3 then 3s); **bars with the whole "?" above and a known amount braced below the shaded parts**; juice bottle story; letter puzzle. Guide: shade the known part(s), find one part, build the whole. | `conversions:find_whole_from_pct` (percent) only → **GAP** R27 `task: 'whole'` + `fractions:find_the_whole`. |
| 7 Use fractions as operators | Paired bars: "5 lots of 1/5" (five cells of 1/5) vs "1/5 of 5" (a bar of 5, one part shaded); 8 lots of 1/4 vs 1/4 of 8; **four rows of 6 cells** (1/6 × 24) vs a bar of 24 in sixths; match calculations with the same answer (4/5 of 45 = 4/5 × 45); true / false. Guide: bar + ÷ denominator, × numerator. | MATCH `fraction_operations:mult_frac_whole`. Paired-bar contrast: PARTIAL R54 / R27. |

## Block 7 — Spring 3 · Decimals and percentages (15 steps)

Our pv drawings stop at the ones place (`pv.js` `LETTER` = O … M) and `stack` returns to the legacy path
whenever `decimalPlaces > 0`, so **every decimal chart, decimal counter and decimal column below is a GAP or a
legacy PARTIAL** until the pv drawing gains places 0.1 / 0.01 / 0.001 (N5-03). The hundred square as one whole
(N5-05) is the other workhorse of this block.

| Step | WRM representations (how drawn) | Match |
|---|---|---|
| 1 Decimals up to 2 decimal places | **A "1 whole" bar of ten equal parts, a 0.1 counter in each**; **hundred square = 1 whole** (sentence frames: one part is one hundredth …); **Ones \| Tenths \| Hundredths chart with counters** (3.14); make numbers with counters; underlined-digit value; partition 0.83 = □ + 0.03 = __ tenths and __ hundredths; **decimal base 10** (flat = 1, rod = 0.1, cube = 0.01); matching clues to decimals. Guide: chart with 0.1 / 0.01 counters. | Chart + counters: **GAP** N5-03. Hundred square: PARTIAL N5-05. Tenths bar with counters: GAP R27 (`fill: 'disks'`). Decimal base 10: GAP R01 (`unit: 'flat'`). Value / partition: PARTIAL `placevalue:value` / `expand` (whole numbers only) → `decimals` option. |
| 2 Equivalent fractions and decimals (tenths) | **A tenths bar: a row of ten 1/10 cells over a row of ten 0.1 counters**; a bar of 7 tenths with counters; **a 10-bead string in two tones** (1 whole, then a part); **double number line 0–1: decimals above, tenths fractions below**; two "one whole" bars of tenths → 1 4/10 = 1.4; double line 1–2; **odd one out across representations** (number line, part-whole, Ones \| Tenths chart, straws, a pizza cut in tenths). Guide: tenths bar & 0–1 line. | `conversions:d_to_f` / `f_to_d` (equations), `fraction_operations:frac_10_100` (visual, legacy). Double line: **GAP** R48 (`labels: ['decimal', 'fraction']`). Bead string: GAP R07. Odd one out: GAP Y1 R48. |
| 3 Equivalent fractions and decimals (hundredths) | **Hundred squares with columns / cells shaded** → fraction and decimal; a square with 3 columns + 4 cells shaded (3/10 + 4/100 = 0.34); **two hundred squares, one full** (1.08); shade squares for 1.4 and 1.04; 117/100 as mixed and decimal; odd one out (squares, counters, number line); digit cards into "□0.□□". Guide: hundred square linked to a tenths · hundredths chart. | PARTIAL N5-05 (`conversions:percent_visual` legacy grid; `frac_10_100`) → kit `template:hundred-square` (`task: 'read' \| 'shade'`, `wholes: 1–2`, labels decimal / fraction). |
| 4 Equivalent fractions and decimals | Shade half a hundred square (1/2 = □/100 = 0.5); shade 1/10, 1/4, 1/5; **double number lines 0–1 / 0–2 / 0–3 (decimals above, fractions below)** with blanks; counting in halves on a double line; arrows on a 0–3 line; convert cards (2/5, 1.1, 13/10, 1 3/4, 2.5); halfway between two arrows. Guide: hundred square, then the double line. | N5-05 PARTIAL; R48 GAP; `decimals:decimal_nl_drag` MATCH for one-row lines (`ticks`). |
| 5 Thousandths as fractions | **A thousand square** (1,000 tiny squares, 13 shaded → 13/1000); cards "22 shaded parts"; 1/10 = □/1000; **PV counters labelled as fractions (1/10, 1/100, 1/1000)** partitioning 342/1000 = 3/10 + 4/100 + 2/1000; flexible partitions; compare circles 9/1000 ○ 8/100; ten 1/1000 counters vs one 1/100 counter. Guide: 1000-grid → base 10 → counters. | Thousand square: **GAP** N5-06 (`template:hundred-square` `grid: 1000`). Fraction-valued counters: GAP N5-03 (`disk: 'fraction'`). |
| 6 Thousandths as decimals | **Exchange diagrams: ten 0.01 counters in a box = one 0.1 counter; ten 0.001 = one 0.01**; counting 0 to 0.1 in hundredths on a line, then **one interval zoomed into ten thousandths** with arrows; 0.254 as counters (0.2 + 0.05 + 0.004) and after exchanging one 0.01 for ten 0.001; **a measuring scale 7.71–7.75 m with thousandth ticks under a bar** (how long is the rectangle?); partitions compared. Guide: chart to thousandths + counters; number line. | Counters: GAP N5-03. Zoomed line: GAP (R17 `zoom` — a second line magnifying one interval). Scale under a bar: PARTIAL R46 (`template:ruler` proposal, `units: 'm'`, decimal labels). `decimals:decimal_nl_drag`: MATCH for plain placement. |
| 7 Thousandths on a place value chart | **Two O · Tth \| Hth \| Thth charts with counters** (the decimal point drawn as a dot on the O / Tth line); charts → sentence frames; make numbers; partition 12/1000 etc.; two partitions of 0.132; **eight plain counters on an empty chart** (at least three columns: greatest / smallest); **a digit chart with digits 0 . 0 0 4 5** (a fraction put in wrongly). Guide: chart O·Tth·Hth·Thth with 0.1 / 0.01 / 0.001 counters. | **GAP** N5-03 (chart + counters + digit chart to thousandths; `placevalue:pv_digit_drag` whole numbers only). Plain dots: GAP R05. |
| 8 Order and compare decimals (same number of decimal places) | 0.6 vs 0.4, 0.14 vs 0.17; make 0.452 and 0.364 on charts; compare circles; **a number line 2.34–2.36 with thousandth ticks**, one arrow drawn, draw the others; ascending order cards; two charts of plain counters (O Tth Hth Thth). Guide: column-aligned chart + counters; line for ordering. | MATCH `decimals:compare_decimal`, `compare_thousandths`, `order_decimals`, `decimal_nl_drag` (`ticks`). Charts: GAP N5-03. |
| 9 Order and compare any decimals with up to 3 decimal places | Two charts with counters (0.31 vs 0.156); **two digit charts** (2.113 vs 2.13); compare circles; charts with plain dots (0.32 vs 0.302); ascending order; **lengths in m** longest → shortest; an ordered list with a missing box (3.015, 3 51/1000, □, 3.105). Guide: column-aligned chart + placeholder zeros. | MATCH `compare_thousandths`, `order_decimals`. Digit chart: GAP N5-03 (R06 for decimals). |
| 10 Round to the nearest whole number | **A tenths line 5.8–7.3 with the two bounding wholes circled**; cards; 3.8 made with counters (3 ones + 8 tenths); **a line 4–5 with two labelled hops (−0.3 back to 4, +0.7 on to 5)** — distance to each bound; a line 6–7 with an arrow at 6.37 (below 6.5); inverse rounding. Guide: bounding wholes + midpoint .5. | MATCH N5-30 `decimals:round_decimals` (legacy visual) / `number_sense:round_sort_tenths`; bounds circled + hops to bounds: PARTIAL (`pane:round-line` whole-number only → `decimals` option, `hops: 'bounds'`). |
| 11 Round to 1 decimal place | A hundredths line 6.09–6.21 with 6.1 and 6.2 circled; cards; counters for 3.43 (ones, tenths, hundredths); **hops to both bounds** (−0.07 / +0.03; −0.08 / +0.02). Guide: bounding tenths + placeholder zeros, midpoint marked. | MATCH `decimals:round_decimals`, `round_thousandths`, `number_sense:round_sort_hundredths`; line PARTIAL as S10. |
| 12 Understand percentages | Hundred square with 1 part shaded = 1%; squares with parts shaded → %; **a "100%" bar split into ten parts, one shaded = 10%**; three 100% bars → __%; a 10% part split in two by a line; draw bars for 15 %, 95 %, 75 % …; 47 of 100 children; **part-whole with percentages (100% → 21%, □, 58%: three parts)**; **a splat over a hundred square** (could be / must be / cannot be); a hundred square vs a 100% bar compared. Guide: each column = 10%; the % bar for benchmarks. | Hundred square %: PARTIAL N5-05 (`conversions:percent_visual`, legacy). % bar: **GAP** N5-07. 3-part cherry: GAP R13. Splat: GAP R87. |
| 13 Percentages as fractions | Six hundred squares with patterns → "__ parts out of 100 = □/100 = __%"; **bars of 10 parts** → "__ out of 10 = □/10 = __%"; **a fraction wall headed 100%** (halves, quarters, fifths, tenths rows); 1/5 = 20% so 2/5 = 40%; missing equivalences; squares showing 100% = 1, 50% = 1/2, 25% = 1/4. Guide: shade __ of 100, write over 100. | MATCH `conversions:f_to_p` / `p_to_f` (equations). Squares: PARTIAL N5-05. Wall with a 100% head: PARTIAL R54 (`stack`, `whole: '100%'`) / GAP N5-07. |
| 14 Percentages as decimals | One 1 counter = ten 0.1 counters **beside a 100% bar of ten 10% cells**; ten 0.01 counters beside a 10% bar of ten 1% cells; 0.1 = __%; **a 100-bead string as 100%** (3 beads = __ = __%); compare circles 90% ○ 0.9; order mixed % and decimals; **part-whole: 1 whole → 21%, 0.5, □**; digit cards 0.□□ < □□% < 3/5. Guide: bar of tenths / hundredths, then the hundred square. | MATCH `conversions:d_to_p` / `p_to_d`, `order_fdp`. Counters: GAP N5-03. % bar: GAP N5-07. Bead string: GAP R07. |
| 15 Equivalent fractions, decimals and percentages | Hundred square half shaded → 50/100, 0.5, 50%; shade for 1/5, 1/10; 92% and 28/100 equivalents; **a 100% bar of four quarter cells**; **a blank 100% bar of five parts with "__%" in each**; **a triple number line 0–1: fractions above, decimals on the line, percentages below**; sweets story; true / false. Guide: hundred square & fraction bar (fifths × 20, quarters × 25). | MATCH `conversions:order_fdp`, `mixed_conversions`. Triple line: GAP R48 (`labels: ['fraction', 'decimal', 'percent']`). % bar: GAP N5-07. |

## Block 8 — Spring 4 · Perimeter and area (6 steps)

| Step | WRM representations (how drawn) | Match |
|---|---|---|
| 1 Perimeter of rectangles | **Short cm rulers (0–3, mm ticks) under lines** to read; unlabelled rectangles drawn at angles to **measure with a ruler** then "__ cm + __ cm + … = __ cm"; draw a rectangle of perimeter 20 cm; labelled rectangles solved two ways (add four sides vs (l + w) × 2) in boxes; a missing side from the perimeter ("?"); an object on a ruler **not starting at 0**; all rectangles of perimeter 24. Guide: l + w + l + w = 2 × (l + w). | Labelled: MATCH R52 `area_perimeter:perimeter`, `perimeter_intro` (`labels`). Missing side: PARTIAL (task option). Ruler: PARTIAL R46 (`measurement:reading_ruler` is inches, legacy → kit `template:ruler` `units: 'cm'`, `start: 'offset'`). Measure-and-add: GAP (needs true-scale print). |
| 2 Perimeter of rectilinear shapes | **L-shape on a squared grid (1 cm key)**; **labelled L-shapes with some sides unlabelled** (double-arrow marks on the unknowns); shapes with a notch (a 5 1/2 cm side); a plus-shape perimeter; "enough information?". Guide: tick each side as you count it. | MATCH R52 `area_perimeter:perimeter_grid`, `composite_shapes` (L / T / U). Find the unknown sides: PARTIAL (`task: 'missing-sides'`). |
| 3 Perimeter of polygons | **Regular triangle / pentagon / hexagon with one side labelled**; **a hexagon tessellation with a shaded region** (perimeter of the shaded shape); a regular octagon from three sides; court, room, pool stories; perimeter → side; a square inside a square; a stage of two rectangles (inner sides excluded). | Regular polygons: GAP → `area_perimeter:perimeter` `shape: 'regular'` (side × n; reverse). Tessellation: GAP (low). |
| 4 Area of rectangles | Arrow vs circle (which greater area?); **shapes on a squared grid (each square 1 cm²)**; a rectangle on a grid with frames "__ rows of __ squares; __ × __ = __"; labelled rectangles (m, mm, cm; one tilted; a square); draw rectangles of area 12 cm²; area 18 cm², width "?"; rectangles 8 × 3 and 6 × 4; a compound of labelled squares with a known area. Guide: array of cm² → l × w; units cm². | MATCH `area_perimeter:area_unit_squares`, `area`, `area_perimeter` (R52 family), `pane:area` / `gridpaper`. Missing side: PARTIAL (task). |
| 5 Area of compound shapes | **Compound shape on a squared grid split by a dashed line into A and B**; labelled rectilinear shapes; **subtract the missing corner** (7 × 5 − 3); three rectangles placed side by side; area given → perimeter; three squares of known area. Guide: partition into labelled rectangles A, B (, C). | MATCH N5-24 `area_perimeter:area_polygon_decompose`, `composite_shapes`. Subtract-the-corner method: PARTIAL (`method: 'subtract'`). |
| 6 Estimate area | **An irregular blob on a 1 m grid** (estimate 8 m²); a blob on a cm grid — count full squares and more-than-half squares; leaves on grids; draw a circle on squared paper; a triangle on a grid (half squares); design an island on a grid (each square 4 m²). Guide: count and mark squares. | **GAP** N5-23 → `area_perimeter:estimate_area` (outline on `pane:gridpaper`, full / half rule; the shape drawn as a smooth closed path, B&W). |

## Block 9 — Spring 5 · Statistics (5 steps)

| Step | WRM representations (how drawn) | Match |
|---|---|---|
| 1 Draw line graphs | **A table beside blank axes** (day \| temperature; two points already plotted); a rainfall table split into two halves beside axes 0–70; a **horizontal table** (month \| temperature); shadow lengths by hour; a £ ↔ rupee conversion table → line graph; a population table; **choosing the vertical-axis interval** (2s, 200s, 1,000s). Guide: plot point by point. | **GAP** N5-19 → new `graphs:line_graph` (tasks read / plot / choose-scale; the table printed as R58). `graphs:line_plot` is a dot plot (different). |
| 2 Read and interpret line graphs | **Line graph of a town's population 1975–2015** (axis 0–100,000 in 10,000s, points joined); **night temperatures 22:00–06:00** (dashed line); read between points; **three small distance–time sketches** matched to stories; a bath-level graph (fill, stay, empty) → write the story. Guide: ruled lines to the axes; difference = subtract two reads. | GAP N5-19 (read task; `ruled: true` hint lines to the axes as a fading scaffold). |
| 3 Read and interpret tables | Two-row tables (colour \| number; city \| population); **city × January / July temperature**; planet table (four numeric columns); sports-day results (names × events); stadium table (true / false). Guide: finger on the row, slide to the column. | **GAP** R58 `template:data-table` + a read-the-table skill (`data_analysis:read_table`). |
| 4 Two-way tables | **Two-way tables with totals**: staff by rank × glasses; pets by type × sex (blanks); travel by key stage (blanks); matches won / lost by team; sandwich bread × filling; **build a two-way table from sentences**. Guide: shade the target row and column; read the crossing. | **GAP** R73 `graphs:two_way_table` on R58 (tasks read / complete-totals / build). |
| 5 Read and interpret timetables | **School timetable grid** (days × periods, times across the top, merged cells, vertical assembly / break / lunch bands); **train timetable** (stations × departures, blanks where a train does not stop); **TV guide grid** (channels × time, programme blocks of different widths); **bus timetable**; walking-time puzzle. Guide: finger-trace; number line for the duration. | **GAP** N5-20 → `template:data-table` `kind: 'timetable'` + `measurement:timetable` (read a departure, journey time via `template:timeline`). |

## Block 10 — Summer 1 · Shape (10 steps)

| Step | WRM representations (how drawn) | Match |
|---|---|---|
| 1 Understand and use degrees | **A top-view child in the middle of four labelled objects** (turn 360 / 180 / 90 and name what they face); degrees in a turn (frames); **8-point compass rose**: turn from / to, clockwise / anticlockwise, in degrees; **an 8-direction star of labelled places**; **table start time \| end time \| degrees with a clock face** (minute-hand turn). Guide: a turnable angle opened to each benchmark. | **GAP** R80 → `angles_lines:turns` (compass / labelled-star diagram, `task: 'face' \| 'degrees'`, `dir: 'cw' \| 'acw'`). Clock turn: PARTIAL R60 `template:clock` (minute hand) + a degrees column. |
| 2 Classify angles | Right-angle square mark and straight-line (dot + arc) benchmarks; example acute / obtuse / reflex angles with arcs; draw two of each; **a quadrilateral-like polygon with interior angles a–g marked by arcs** to classify; degree cards sorted acute / obtuse / reflex; draw a triangle and a quadrilateral and label angle types. | PARTIAL R74 `angles_lines:identify_angles` (legacy; reflex?) → kit `template:angle` (`arc`, `right-mark`), sort task R59. |
| 3 Estimate angles | Three angles with a choice of two estimates each; **right angles cut by a dashed diagonal** (45°); match angles to estimate cards; **a fan of lines from one point** (estimate each angle from the base); angles p, q, r; a concave shape; draw angles of about 40 … 240°. Guide: halve a right angle → 45; + half → 135. | PARTIAL R74 `angles_lines:measure_angles` (MC estimate among 30° / 45° multiples, legacy) → options `benchmark: 'diagonal'`, fan item. |
| 4 Measure angles up to 180° | **A semicircular protractor (0–180 double scale, fine ticks) drawn over an angle**, one ray on the baseline, the other crossing the scale; a protractor where the angle does not start on 0 / uses the other scale; angles to estimate then measure; crossing lines (vertically opposite); wrong-scale error (30 vs 150). Guide: outer & inner scales. | **GAP** N5-14 → `template:protractor` + `angles_lines:read_protractor` (`scale: 'inner' \| 'outer'`, `start: 'zero' \| 'offset'`); true-scale print for measuring with a real protractor. |
| 5 Draw lines and angles accurately | Line lengths to draw (12 cm, 6.5 cm, 75 mm, 105 mm); **a protractor on a horizontal line with a pencil mark** (what angle is being drawn?); draw 45°, 15°, 110°, 155° from a line; draw from both ends of a segment; **construct shapes from given sides and angles** (4.2 cm, 65°, 3 cm); a non-square on a ruler; a 200° angle two ways. Guide: ruler + protractor 4-step routine. | GAP N5-14 (read-the-mark task) and R46; drawing tasks are hands-on (paper only, true scale). |
| 6 Calculate angles around a point | A turning figure (complete turn = __ right angles = __°); fractions of a turn; **a full-circle (360°) protractor behind four angles a–d**; **circles split into sectors with angle arcs** (238° and p; q, 137°, 157°; six equal r); x among 55°, 80°, 15°; three angles by relations; 360 − 170 = 190 error (a right angle missed). Guide: sectors → a bar with whole 360. | **GAP** N5-15 → `template:angle-sum` (`around: 360`, 2–6 parts, one or more unknown letters); `angles_lines:additive_angles` (legacy, two parts of 90 / 180 / 360, text only) PARTIAL. Circle protractor: GAP N5-14 `full: true`. |
| 7 Calculate angles on a straight line | A figure turning half a turn; **semicircle diagrams of angles on a straight line** (a and 33°); lines meeting on a line (37° and 127°; right angle + 33°); several angles on one line; **five equal angles x**; **two crossing sticks** (vertically opposite p, q, r with 66°); angle-sum sentences. Guide: straight line → bar with whole 180. | GAP N5-15 (`around: 180`, `cross: true`); PARTIAL `additive_angles`. |
| 8 Lengths and angles in shapes | Rectangle 4 × 3 cm area and perimeter; **compounds of three / four rectangles in a row**; a rectangle split by a diagonal (25°, a); **triangles with exterior / reflex angles marked**; isosceles trapezium perimeter 44 with y = 2x; a triangle drawn in a rectangle on squared paper; **equilateral-triangle stickers tiled into a compound shape** (perimeter, angles a, b, c). Guide: mark every known fact on the shape first. | Area / perimeter: MATCH R52. Angles in shapes: GAP N5-15 (`shape` variant). |
| 9 Regular and irregular polygons | Which are polygons (open shape, dashed, curved, a cube, a semicircle); **shapes A–G with equal-side hatch marks and right-angle marks** → regular / irregular; perimeters of regular shapes; a house-pentagon with equal sides (not regular); always / sometimes / never. Guide: sides ✓ then angles ✓. | PARTIAL R78 `shapes_classify:classify_quads`, `shapes_early:shape_attributes` → hatch-mark / angle-mark options + `task: 'regular'`. |
| 10 3-D shapes | **Match pictured solids to names** (sphere, cuboid, triangular pyramid, hexagonal prism, cone, cylinder); faces / edges / vertices of a cube, sphere, tetrahedron; **solids drawn on isometric dot paper** (prism, cube, pyramid); **compound solids on isometric paper**; a hexagon-looking cube; faces of a compound shape. Guide: number each face and vertex. | MATCH R79 `shapes_early:name_3d_shapes`, `count_edges_faces_vertices`. Isometric: **GAP** N5-13 (`template:iso-grid`). |

## Block 11 — Summer 2 · Position and direction (6 steps)

| Step | WRM representations (how drawn) | Match |
|---|---|---|
| 1 Read and plot coordinates | **First-quadrant grids (0–8, 0–10)** with points marked × and lettered → write coordinates; which point is (7, 3)?; plot cards on a blank grid and join them into a polygon; the other vertices of a square from two given; points on a vertical line; one vertex of a rectangle with a given perimeter. Guide: across, then up. | PARTIAL N5-16 `coordinates:coordinate_q1`, `coordinate_graph`, `coord_polygon` (legacy SVG grid) → kit `template:coord-grid` (`task: 'read' \| 'plot' \| 'join'`, `max` 8 / 10 / 20, `every` 1 / 2). |
| 2 Problem solving with coordinates | **A rectangle on axes with no grid lines** (only one corner labelled) → the other corners; a rectangle with two opposite corners labelled; a square of area 9 from one vertex on a grid; an L-shape with three labelled vertices; points whose coordinates add to 6 (line); a rectangle of perimeter 14 from one vertex. Guide: trace each side, count squares. | PARTIAL N5-16 (`grid: false` axes-only variant; missing-vertex task). `coordinates:coord_distance_q1`: MATCH for side lengths. |
| 3 Translation | **Points on a plain squared grid (no axes)** — translate "2 squares right" etc.; shapes (rectangle, triangle, house-pentagon) on a grid to translate; describe translations between plotted points; triangles on a coordinate grid — "translated __ squares left / right and __ up / down" frame; an arrow shape translated as far as possible. Guide: count across then up / down from corresponding vertices. | PARTIAL N5-17 `coordinates:geo_translate` (legacy, MC) → kit `template:coord-grid` `task: 'translate'` (draw the image / describe with a two-blank frame). |
| 4 Translation with coordinates | Small grids (0–8 × 0–4): A → B, write both coordinates; **table coordinates \| translation \| new coordinates** (first row done, blanks elsewhere); a triangle translated so one vertex lands on a marked point → the other vertices; a translated rectangle → the original (axes 0–20 in 2s). Guide: x first, then y. | PARTIAL N5-17 + GAP R58 (table layout). |
| 5 Lines of symmetry | A rectangle on a grid with a dashed symmetry line (any others?); shapes A–F on squared paper — which have a line of symmetry (vertical / horizontal / diagonal); a cross shape with lines drawn; add lines to a shape; **2 × 2 Carroll-style table (1 line / more than 1 × up to 4 sides / more than 4 sides)**; draw shapes with 0 / 1 / 2+ lines on squared paper; add or remove a square to change the number of lines. | PARTIAL R76 `angles_lines:symmetry`, `place_symmetry_lines` (legacy). Carroll: GAP R44. Draw on squared paper: GAP R96. |
| 6 Reflection in horizontal and vertical lines | Which pairs show a reflection (A, B, C with dashed mirror lines); **shapes on squared grids beside a dashed vertical or horizontal mirror line** → reflect (squares, L-shapes, triangles, an arrow); a triangle on a coordinate grid reflected in a horizontal line → image vertices; **a rectangle with labelled corners reflected in a vertical line, no grid**; shade two more squares to complete a reflection; squares of known area reflected. Guide: count squares per vertex, plot, join. | PARTIAL N5-18 `coordinates:geo_reflect` (legacy, MC) and GAP R96 (Y2 symmetry completion) → one kit `template:coord-grid` / `symmetry-grid` `task: 'reflect'` (`mirror: 'v' \| 'h'`, `grid: 'squares' \| 'axes'`). |

## Block 12 — Summer 3 · Decimals (12 steps)

| Step | WRM representations (how drawn) | Match |
|---|---|---|
| 1 Use known facts to add and subtract decimals within 1 | **Hundred squares with columns shaded in two tones, read first as ones then as hundredths** (the same picture, two scales); hundred square for 0.72 + 0.13; **Ones \| Tenths \| Hundredths chart with counters** (3 tenths less, 0.02 more); a known fact scaled (86 − 24 = 62 so 0.86 − 0.24 = 0.62); two answers compared; pictured pencil and rubber prices in pence. Guide: chart + scaled known fact. | `decimals:add_decimal` / `sub_decimal` (equations). Hundred square: PARTIAL N5-05. Chart: GAP N5-03. Scaled facts: GAP (low; `related-fact` ladder, cf. Y2 R90). |
| 2 Complements to 1 | **A ten frame whose squares are tenths** (3 counters + __ tenths = 1 whole); 0.8 + □ = 1; **a hundred square of hundredths** (0.55 + □ = 1); a worked chain (763 + 237 = 1,000 → 0.763 + 0.237 = 1); complement cards; **part-whole models with whole 1 and decimal parts (one, two or three decimal places)**; flour-in-kg story. Guide: hundred square + bond to 100 → ÷ 10 twice. | Ten frame of tenths: GAP R10 (`unit: 0.1` on `pane:tenframe`). Hundred square: PARTIAL N5-05. Decimal part-whole: PARTIAL R11 (`template:bond` with decimal values — check the payload formats decimals; `composing:number_bonds` is whole numbers) → new `decimals:complement_to_1`. |
| 3 Add and subtract decimals across 1 | **Two ten frames of 0.1 counters, three moved across with arrows to fill the first** (0.7 + 0.5 = 1 + 0.2); **a number line bridging 1 with two back hops** (1.3 − 0.3 → 1, − 0.4, answer box); **a make-1 partition: the second number split into two parts under the sum, a loop linking the pair that makes 1** (0.45 + 0.67; 0.67 → 0.55 and 0.12); partition for subtraction (1.3 − 0.8); a dice game into a "0.□□" box template. Guide: line bridging 1 + a partition diagram. | Two ten frames: GAP R10 (`frames: 2`, `unit: 0.1`). Line: PARTIAL R20 (decimal open line). Split: **GAP** R21 (`pane:split` with decimal values). |
| 4 Add decimals with the same number of decimal places | **O \| Tth \| Hth chart with two rows of counters beside a column addition on squared paper with the decimal point in its own narrow column**; an exchange version (ten tenths ringed); cards; **price tags on pictured items** (£4.85, £9.15 …); a misaligned 49p error; digit cards into a column box template (greatest / smallest sum). Guide: chart aligned on the point + counters beside the column. | Column with a point track: PARTIAL R30 — `template:money-columns` already draws a stack with the decimal point in its own narrow track through every row (2 dp, money, `currency`), so £ items MATCH; plain decimals do not reach it (`template:stack` returns to legacy when `decimalPlaces > 0`) → lift the point track into `stack` as `dp: 1–3`. Chart beside: GAP R31. Price tags: PARTIAL R69. |
| 5 Subtract decimals with the same number of decimal places | O \| Tth \| Hth charts of counters beside column subtraction (with and without exchange); column subtractions with points; money and cycling stories; **an L-shaped hexagon with decimal side lengths** (two unknown sides); **a number pyramid of decimals**. | As S4 (GAP N5-03 / R31). Pyramid: GAP N5-11. Missing sides: PARTIAL R52 (`composite_shapes` whole numbers). |
| 6 Add decimals with different numbers of decimal places | O \| Tth \| Hth (\| Thth) charts beside the column; mixed-dp column additions (placeholder zero); **a bar model with a blank whole over 4.35 \| 12.7**; measures cards (m, litres, kg, km); a misaligned-digit error; **a five-column sort table "no exchange \| exchange in ones \| tenths \| hundredths \| thousandths"**; a digit box template □.□ + □.□□□ = 3.678. | As S4. Bar: PARTIAL R25. Sort: PARTIAL R59. |
| 7 Subtract decimals with different numbers of decimal places | Charts of counters beside column subtraction (4.54 − 1.4; 6.5 − 4.23 written as 6.50); mixed-dp columns; change from £10; measures in g / kg, cm / m, ml / l with blanks; a misalignment error; money chains; **an L-shape of two identical rectangles** (find a side). | As S4 / S6. |
| 8 Efficient strategies for adding and subtracting decimals | A T \| O \| Tth \| Hth chart of counters beside a column (could it be mental?); **a jump line with two labelled hops** (4 + 3.75: +3, +0.75; counting back 20.7 − 2.5: −2, −0.5); mental / line / column compared; **a three-column sort "mental \| informal jotting \| formal written"**; missing digits in a column; compensation with 9.9 (−10 + 0.1). Guide: line (partition & count on / back); round & adjust. | Line: PARTIAL R20 (decimal hops). Sort: PARTIAL R59. Compensation: PARTIAL `number_sense:compensation` (whole numbers). |
| 9 Decimal sequences | **A sequence of counter pictures as terms** (1st, 2nd, 3rd term as growing columns of 1 / 0.1 / 0.01 counters, values below) → 4th, 5th; **number lines with some decimal labels** (2.4, 2.6, 2.8 …; 4.67, 4.69, 4.71 …) to complete; rule for a sequence; missing terms from a rule (+0.3, +0.25, −1.1); a library fine (£1.50 + 15p a day); cards to order into a sequence. Guide: chart + line with the step above each jump. | Tracks / rules: PARTIAL R22 `template:count-row` (`patterns:count_by_step_up/down` whole steps → `step` 0.1 / 0.01 / 0.25). Labels on a line: PARTIAL R17 (`decimals:decimal_nl_drag`). Counter terms: GAP N5-28 (low). |
| 10 Multiply by 10, 100 and 1,000 | **Ten identical rows of counters (3 ones + 2 tenths)** = 3.2 × 10 (exchange); **H \| T \| O . Tth \| Hth chart of dots** (× 10 moves the counters left); cards 4.24, 2.401; **a Gattegno chart 1,000s … 0.01s with two cells ringed and arrows up one row** (4.9 × 10 = 49); Gattegno multiplications; compare circles 4.732 × 100 ○ 47.32 × 10; money × 10. | PARTIAL `placevalue:place_value_10x` (`decimals: true`, `support: 'shift'`) — its decimal level is one decimal place ("3.4 × 100"); Year 5 uses 2–3 places (4.24, 2.401) → `dp: 1–3`. Dots: GAP R05 / N5-03. Gattegno: GAP N5-04 (`rows` down to 0.001). |
| 11 Divide by 10, 100 and 1,000 | T \| O . Tth \| Hth \| Thth chart of dots for 14 (÷ moves right); counters to divide 4, 15, 301; **a digit chart H \| T \| O \| Tth \| Hth \| Thth** beside 27 ÷ 10 / 100 / 1,000; **Gattegno with arrows down one row** (5.8 ÷ 10); pence → pounds (210p = £__); **three scaled rectangles** (B ten times A, C one hundredth). | PARTIAL `place_value_10x` (`op: 'divide'`, `decimals` — one decimal place today; 27 ÷ 1,000 = 0.027 needs Thth) → `dp: 1–3`. Digit chart to thousandths: PARTIAL R06 (shift chart without Hth / Thth heads). Gattegno: GAP N5-04. Money: MATCH `measurement:money_notation` (`currency`). |
| 12 Multiply and divide decimals — missing values | **A digit chart with curved arrows showing each digit moving one place** (4.23 × □ = 42.3); counters for missing values; "what did he start with?" from a digit chart (0.52); **Gattegno: 4.82 × □ = 482 with rings and arrows up two rows**; missing-value frames; inverse "think of a number". Guide: function machine (× ↔ ÷) + digit shift. | PARTIAL `place_value_10x` (the unknown is the product, not the power) → `unknown: 'power' \| 'start'` option. Machine: PARTIAL R36. Gattegno: GAP N5-04. |

## Block 13 — Summer 4 · Negative numbers (5 steps)

| Step | WRM representations (how drawn) | Match |
|---|---|---|
| 1 Understand negative numbers | **A lift button panel** (2, 1, 0, −1 in a column; a hand covering one); **four vertical thermometers** (°C, mercury column) to read; **a sea-level cross-section with a vertical metre scale on both sides** (+5 m … −8 m: a bird's height, a fish's depth); **table time \| temperature** with clue sentences; a mislabelled thermometer; two small thermometers (a temperature between). Guide: vertical thermometer + a horizontal line through 0. | Thermometer: PARTIAL R98 `measurement:temperature` (`createThermometerSVG`, B&W, vertical, −10 … 40 °C) → kit `template:thermometer`. Vertical line / sea level / lift: **GAP** N5-21 (`orientation: 'vertical'` on `integers:number_line_int`'s value-line). Table: GAP R58. |
| 2 Count through zero in 1s | **Vertical number lines with some labels** (4 … −2; −1 … −9) to complete; **horizontal lines through 0 with gaps** (−2 … 5; −11 … −1; −8 … 2); sequences −20, −19, …; **arrows to boxes on lines with only 0 and 2 (or 0 and 5) labelled**; temperature 5 degrees colder; counting-sequence errors; a thermometer. Guide: count in 1s across zero, horizontal & vertical. | Horizontal: MATCH N5-21 `integers:number_line_int` (`template` value-line, `ticks` 'one' / 'ends'), `integer_nl_drag`. Vertical: GAP N5-21. Sequences: PARTIAL R22 `count-row` (negative starts?) → `patterns:count_by_step_down` through 0. |
| 3 Count through zero in multiples | Vertical lines counting in 2s / 3s / 5s through 0 with blanks; sequences −16, −12, −8 …; **a −12 … 12 line** for sequences; **counting back in 3s from 2 with arcs, partitioning 3 into 2 and 1 to land on zero** (ringed −1 and 2); a diver descending / ascending; five cards from a counting pattern (find the missing card). Guide: equal jumps, partition to land on 0. | PARTIAL R19 `template:hop-line` (`patterns:skip_count_line` from 0 only) → negative start + `bridge: 0`. Horizontal line: MATCH `number_line_int`. |
| 4 Compare and order negative numbers | 0–10 line + compare circles; "numbers to the left are __"; **horizontal thermometers (−5 … 5)** with word cards warmer / colder, less / greater; a −10 … 10 line + compare circles; **a horizontal thermometer −12 … 12 °C** to order temperatures; ascending / descending cards; "4 > 1 so −4 > −1" error → draw a line; −3 < □ < 2 (all answers); lift floors; table city \| temperature. Guide: horizontal line through 0; thermometer higher = warmer. | MATCH `integers:compare_int`, `order_negatives`, `number_line_int`. Horizontal thermometer: PARTIAL R98 (`orientation`). Word bank: PARTIAL Y1 R16. |
| 5 Find the difference | **A −5 … 5 line with unit hops from −2 to 3** (ends ringed) = 5; **a −10 … 10 line with two big arcs, −8 to 0 (8) and 0 to 9 (9)** → 8 + 9 = 17; count the jumps from zero to each card on 0 … 10 and −10 … 0 lines; London vs Moscow temperatures; car-park levels; **table city \| highest \| lowest**. Guide: split the jump at 0. | `integers:sub_int` (equations) → PARTIAL R20 (open line with hops split at 0, `bridge: 0`). Table: GAP R58. |

## Block 14 — Summer 5 · Converting units (6 steps)

| Step | WRM representations (how drawn) | Match |
|---|---|---|
| 1 Kilograms and kilometres | **Two-row conversion bars** (2 km as two "1 km" cells over two "__ m" cells; 4,000 g as four "1,000 g" cells over "__ kg"); **double number line kg over g** (boxes to fill); **double line 0–1,000 m (100 m ticks) over 0–1 km** (½ km, ¼ km = __ m); compare circles 5 kg ○ 4,500 g; fractions of a unit (1/10 kg = __ g); **a see-saw balance** (2 kg vs cubes of 250 g); potatoes priced per kg; sponsored run (25p per 100 m). Guide: conversion ladder / double line; × 1,000 down, ÷ 1,000 up. | Conversions: MATCH `measurement:unit_conversions` (visual, legacy), `length_metric`, `unit_conversion_word`. Bars: **GAP** R29 (`model: 'bars'`). Double line: **GAP** R48 (`template:double-scale`). Balance: PARTIAL R50. |
| 2 Millimetres and millilitres | Conversion bars (four 1,000 ml cells over "__ l"; six "1 m" cells over "__ mm"; 2 ½ l = __ ml); **double line 0–1,000 ml over 0–1 l** (200 ml = □/□ l); a fact box (1,000 mm = 1 m); compare circles; a juice jug; a ribbon of 5 m as a bar of 4 parts in mm; a can (330 ml, 48p) and a bottle (1.25 l, £1.59). Guide: double line + digit shift. | As S1: GAP R29, R48; MATCH `measurement:capacity`, `mass_volume_liquid` (legacy). |
| 3 Convert units of length | **A conversion ladder: boxes mm → cm → m with curved "÷ __" arrows above and "× __" arrows below** (and a direct mm → m arrow); missing conversions; **coloured lines to measure (cm and mm)**; a heights table (1.3 m, 124 cm …) to order; compare circles 55 mm ○ 6 cm; draw lines A, B, C from relations; a plank of 5.8 m cut into 175 cm and 3 4/5 m; a pile of 10p coins 2 mm thick. Guide: ladder + double line + digit shift. | MATCH `measurement:length_metric` (mm / cm / m / km). Ladder: **GAP** N5-22 (a structural scaffold pane `ladder`). Measure lines: PARTIAL R46. |
| 4 Convert between metric and imperial units | Fact cards (1 inch ≈ 2.5 cm; 1 kg ≈ 2.2 lb; 1 pint ≈ 568 ml) → conversions; a rectangle of area 50 cm² with a 5 cm side → perimeter in inches; apples in 2 kg bags vs lb; **a gallon bar model (two "1 gallon" cells over pints over ml, total "?")**; birth masses in lb vs kg; milk in pints per week. Guide: double line scaling the given fact. | `measurement:length_customary` (customary only). **GAP** → `measurement:metric_imperial` (approximate facts; US grade 4 has customary ↔ customary instead, 4.MD.A.1). Bars: GAP R29. |
| 5 Convert units of time | **Double number line hours (0, 1, □, 3, □) over minutes (0, □, 120, □, □)**; 60 seconds in a minute; boiling an egg (4 ½ min + 45 s); **table Days \| Weeks and days** (42 days / 5 weeks and 3 days …); months ↔ years; the 60 × 60 error; age in days (leap years); race times in mixed units. Guide: ladder + double line (weeks / days, years / months). | PARTIAL `measurement:unit_conversions` (time?) → time units on R48 `template:double-scale`; table GAP R58. Y3 R29 (60-s cells) is the bar form. |
| 6 Calculate with timetables | **A number line 07:51 → 08:00 (+9 min) → 09:00 (+1 hour) → 09:17 (+17 min)** with three labelled hops; **bus timetable** (stops × times, blanks where a bus does not stop, a bus icon header); **train timetable**; **a TV guide drawn as a horizontal timeline 11:00–17:00 with programme blocks of different widths**; multi-step with walking times. Guide: jump to the next hour, then the extra minutes. | Duration line: MATCH R64 `template:timeline` (`measurement:elapsed_find_duration`, modes duration / start). Timetables: **GAP** N5-20. TV-guide timeline: GAP N5-20 (`template:timeline` with blocks, low). |

## Block 15 — Summer 6 · Volume (4 steps)

| Step | WRM representations (how drawn) | Match |
|---|---|---|
| 1 Cubic centimetres | **3-D drawings of shapes built from linking cubes** (how many cubes?); L-shapes of cubes (volume in cm³); small cube builds; **cuboids of cubes** (a 1 × 1 × 5 tower, flat slabs) → volume; **a T-shape on isometric dot paper**; a cuboid with hidden cubes (counting only the visible ones is wrong); layers × cubes per layer (8 × 2). Guide: build and count the cubes that fill the solid. | PARTIAL N5-13 `area_perimeter:volume` (rectangular prisms, legacy 3-D box, l × w × h), `volume_composite` (legacy) → kit `template:cube-solid` (`task: 'count' \| 'layers'`, isometric B&W unit cubes, hidden-cube rule). Isometric paper: GAP N5-13. |
| 2 Compare volume | A long flat shape vs a tall one (taller is not greater); pairs of cube shapes in boxes (which greater?); **compare circles between pairs of cuboid pictures**; cuboids drawn on isometric paper; stepped shapes (a volume between two others); **two shapes built from different-sized cubes** (not comparable by count). Guide: cube-built solids; layers × cubes per layer. | PARTIAL N5-13 + MATCH R34 compare circle. |
| 3 Estimate volume | A book beside a cuboid of cubes (estimate); an apple beside a cube block; a pyramid beside a stepped cube pyramid; why not cm³ for a room (m³); estimate everyday volumes; a money box partly filled with cubes (depth ignored); marbles in boxes (0.8 cm³ each). Guide: one layer × number of layers. | GAP (N5-13 `task: 'estimate'`, low; pictures are photographs of real objects). |
| 4 Estimate capacity | **Pairs of containers (glasses, beakers, flasks, cones) with the same amount of juice** → greater capacity; capacity cards for a bottle (20 ml / 200 ml / 2 litres / 20 litres) and a teacup; **three containers each holding 1 litre at different levels** (estimate capacity); **a bottle poured into two 250 ml measuring jugs** (scale); four 1-litre containers, estimate each volume of water; jugs A / B with 500 ml at different heights; 1 cm³ of water = 1 ml = 1 g; compost bags (4.5 litres, £2.50). Guide: jug with a scale + a 1-litre bottle benchmark. | Jugs: PARTIAL R51 (`measurement:mass_volume_liquid` graduated cylinder, legacy → kit `template:jug`). Unscaled containers: GAP Y1 R62. Unit choice: MATCH `measurement:capacity`. |

---

## Every Year 5 representation (de-duplicated)

"Where" lists the Year 5 blocks / steps that use it. "Earlier" = the same representation in an earlier
table (`year-3.md` R01–R84, `year-2.md` R85–R101, `year-1.md` "Y1 R.."), or **NEW** when none of Years 1,
2 and 3 has a row for it. Status is the best match across the app **for the Year 5 use** (number range,
decimals, orientation), so a Y3 MATCH can be a Y5 PARTIAL.

| # | Representation | Where (Y5) | Earlier | Status | Existing / proposed |
|---|---|---|---|---|---|
| R01 | Base-10 blocks to scale incl. the thousand cube; **decimal base 10** (flat = 1); inside area-model cells | B1 S2; B5 S2; B7 S1, S5 | Y3 | PARTIAL | `pane:base10` (to 99, not wired) → thousands cube, `unit: 'flat'` (flat = 1), `task: 'read'` |
| R03 | Place-value counters loose / in a row (to 100,000; decimal counters) | B1 S2, S8, S10; B2 S2; B7 S5–S6 | Y3 | MATCH / PARTIAL | `template:pv` `disks` (`place_value_disks` band ≤ 9,999) → 99,999 / 999,999 bands; decimals → N5-03 |
| R04 | PV chart with counters, read / build (TTh, HTh; beside a calculation) | B1 S2–S4, S7, S10; B2 S1–S3; B3 S8–S9; B5 S1, S7–S9, S11; B7 S1, S7–S9; B12 S1, S4–S8, S10–S12 | Y3 | PARTIAL | `template:pv` `build` (`pv_disks_build` ≤ 999) → 5–6 digit bands; beside a calc → R31 |
| R05 | Chart with **plain unlabelled dots** (value from the column; slide left / right) | B1 S2–S7, S10; B2 S1; B3 S8–S9; B7 S7–S9; B12 S10–S11 | Y3 | GAP | `disk: 'value' \| 'dot'` on the pv mat; `motion: 'left' \| 'right'` arrows for × / ÷ 10 |
| R06 | Digit PV chart (digits in heads; the shift chart) | B3 S9; B7 S7, S9; B12 S11–S12 | Y3 | MATCH / PARTIAL | `_pvDigitSupport` `support: 'chart'`; `place_value_10x` `support: 'shift'` (1 dp) → Hth / Thth heads |
| R07 | Bead string (10 beads = 1; 100 beads = 100 %) | B7 S2, S14 | Y3, Y1 R29 | GAP | `pane:beadstring` (`unit` 0.1 / 1 %) |
| R10 | Ten frame of PV counters — **tenths** (0.1 in each square; two frames) | B12 S2–S3 | Y3 | GAP | `unit: 0.1 \| 1 \| 10 \| 100`, `frames: 2` on `pane:tenframe` |
| R11 | Part-whole, 2 parts (numerals, split at the comma, decimals to make 1, Roman parts as text) | B1 S1, S4–S5; B2 S5; B5 S7, S10; B7 S2; B12 S2 | Y3 | MATCH / PARTIAL | `template:bond`; decimal / text values PARTIAL (`values: 'decimal' \| 'text'`) |
| R13 | Part-whole, 3 parts (numbers; percentages; decimals) | B1 S8; B7 S12, S14 | Y3 | GAP | `parts: 2…5` on `template:bond` |
| R15 | Split-and-operate part-whole (÷ arrows under each part) | B5 S7, S10 | Y3 | GAP | `template:split-op` |
| R16 | Area model — 2 × 2 grid (numbers; base 10 or PV counters in the cells) | B5 S2–S3, S5 | Y3 | PARTIAL | `multiplication:area_model_mult_hard` (legacy 2×2 / 2×3); kit `template:area-model` is 1-digit × n → `rows`, `fill: 'base10' \| 'disks'` |
| R17 | Number line to read / label / arrows (to 1,000,000; decimals to 0.001; zoomed interval) | B1 S3, S9, S11; B7 S2, S4, S6, S8; B12 S9; B13 S2 | Y3 | PARTIAL | `pv` `line-mark`, `decimals:decimal_nl_drag` (`ticks`) → `number_sense:read_number_line` (Y3 proposal) with 6-digit and decimal intervals, `zoom` |
| R18 | Estimate on a line (ends only / midpoint only; midpoint between two fractions) | B1 S9, S11, S13; B4 S14 | Y3 | PARTIAL | `ticks: 'none' \| 'mid'` + `task: 'estimate' \| 'midpoint'` on read_number_line |
| R19 | Skip-count / equal-jump line (fraction lots; through zero) | B5 S10; B13 S3 | Y3 | PARTIAL | `template:hop-line` (from 0, whole steps) → negative start, `bridge: 0`, unequal "chunk" arcs |
| R20 | Jump line with labelled hops (compensation ±; bridge 1 with decimals; count on to find a difference; split at 0) | B2 S1, S8; B4 S14–S15; B7 S10–S11; B12 S3, S8; B13 S5 | Y3 | PARTIAL | `pane:openline` (not wired); `number-line` 0–20 → `line: 'open'`, `hops: 'compensate' \| 'bridge'`, decimal / fraction / integer values |
| R21 | Split under the second number to make a whole (0.67 → 0.55 + 0.12) | B12 S3 | Y3 | GAP | `pane:split` with decimal values |
| R22 | Number / sequence track (Roman, mixed numbers, decimals, 10,000s) | B1 S1, S7; B4 S13; B12 S9 | Y3 | MATCH / PARTIAL | `template:count-row` / `seqstrip` (`count_by_step_*`, `count_by_powers_of_10`) → fraction / decimal / text tiles |
| R23 | Hundred square — shade multiples, circle a second set, the sieve | B3 S1–S2, S5 | Y3 | PARTIAL | `pane:hundreds`, `template:chartwindow` → whole-chart `task: 'shade' \| 'circle' \| 'sieve'` |
| R25 | Bar model part-whole (whole over parts; fact family; decimals) | B1 S3–S4; B2 S5–S6, S8; B5 S6, S11; B12 S6 | Y3 | PARTIAL | `template:word-work` `wpBar`, `pane:bar` (not wired), `algebra:tape_diagram` (legacy) |
| R26 | Comparison bars (stacked, left-aligned; brace for the total, difference marked) | B2 S6–S7 | Y3 | MATCH / PARTIAL | `wpBar` compare look → `brace: 'total'` |
| R27 | Equal-parts bar with counters / PV counters / numerals in the parts; "?" whole (find the whole) | B6 S4–S7; B7 S1 | Y3 | GAP | `model: 'bar'` + `fill: 'counters' \| 'disks' \| 'numbers'`, `task: 'part' \| 'whole'` → `fractions:fraction_of_amount`, `find_the_whole` |
| R29 | Unit-conversion bars (1 km cells over "__ m"; gallons over pints over ml) | B14 S1–S2, S4 | Y3 | GAP | `model: 'bars'` on `measurement:unit_conversions` / `length_metric` |
| R30 | Column method on squared paper (5–6 digits; 4-digit × 1-digit; decimals with a point track) | B2 S2–S3; B3 S7; B5 S1; B12 S4–S8 | Y3 | MATCH / PARTIAL | `template:stack` (whole numbers; bands to 10,000); point track exists in `template:money-columns` (£) → `stack` `dp: 1–3` |
| R31 | PV chart beside the method with exchange ring + arrow (add, subtract, n rows for ×, grouped rings for ÷) | B2 S2–S3; B5 S1, S7–S9, S11; B12 S4–S7 | Y3 | GAP | `pane:pv-exchange` (`op` + − × ÷, decimal places) |
| R32 | Crossed-out parts (fraction bars, area rectangles, whole squares) | B4 S9, S14–S17 | Y3 | PARTIAL | `frac-model` → `cross: k` (hatched or X over parts; a big X over a whole) |
| R33 | Missing-digit boxes (column, bus stop, splat-hidden digits) | B2 S2–S3, S8; B5 S1, S5, S7–S9; B12 S8 | Y3 | MATCH | `add_missing_digit`, `sub_missing_digit`, `mult_missing_digit`; bus stop → N5-10 |
| R34 | Compare circle (< > =) between numbers, expressions, measures, pictures | B1–B4, B5 S5–S6, S8; B7; B12 S10; B13 S4; B14 S1–S3; B15 S2 | Y3 | MATCH | equation frame (sign in a circle); `template:pv` `compare` |
| R36 | Function machine / arrow chain (÷100 then ÷2; × ↔ ÷ inverse) | B3 S10; B12 S12 | Y3 | PARTIAL | `template:function-table` → chained look |
| R37 | Equal groups (objects ringed) | B6 S4 | Y3 | MATCH | `template:arrays` groups |
| R38 | Arrays (split with dashed lines; n × n square arrays; factor arrays) | B3 S1, S3–S4, S6; B5 S10; B6 S4 | Y3 | MATCH / PARTIAL | `template:arrays`, `pane:array`; split line PARTIAL |
| R41 | Linking-cube towers (improper fractions: towers of 5) | B4 S4–S5 | Y3, Y1 R11 | GAP | `objects` kind `cubes` |
| R43 | Venn diagram (multiples / factors of two numbers) | B3 S2, S4 | Y3 | GAP | `template:venn` + `number_theory:common_multiples` / `common_factors` |
| R44 | Carroll diagram 2 × 2 (multiples; prime × even; symmetry × sides) | B3 S2, S5; B11 S5 | Y3 | GAP | `template:carroll` |
| R46 | Ruler cm / mm (short rulers, offset start; decimal-metre scale under a bar) | B7 S6; B8 S1; B10 S5; B14 S3 | Y3 | PARTIAL | `measurement:reading_ruler` (inches, legacy) → kit `template:ruler` `units`, `start`, decimal labels |
| R48 | Double / triple number line (kg\|g, m\|km, ml\|l, h\|min; decimal\|fraction; fraction\|decimal\|percent) | B7 S2, S4, S15; B14 S1–S2, S5 | Y3 | GAP | `template:double-scale` (`rows: 2 \| 3`, value kinds per row) |
| R50 | Balance / see-saw with weights | B14 S1 | Y3, Y1 R61 | PARTIAL | `measurement:heavier_lighter_visual` → `template:balance` |
| R51 | Measuring jugs / containers with a scale | B15 S4 | Y3 | PARTIAL | `mass_volume_liquid` (legacy) → `template:jug` |
| R52 | Perimeter / area on a squared grid; labelled rectilinear shapes; missing sides; regular polygons | B8 S1–S5; B10 S8; B12 S5 | Y3 | MATCH / PARTIAL | `area_perimeter:perimeter_grid`, `perimeter`, `area_unit_squares`, `composite_shapes`; missing-side / regular tasks PARTIAL |
| R53 | Fraction area models (rectangles in strips / grids; circles; two-way split) | B4 S1–S3, S5, S10, S14 | Y3 | PARTIAL | `frac-model` area / circle (`model` option) → `split: [a, b]`, several wholes |
| R54 | Fraction bars: stacked equal-length, wall rows, past one whole, mixed-number bars (wholes + part), brackets / braces, 100 % head | B4 S1–S17; B6 S1–S3, S7; B7 S13 | Y3 | PARTIAL | `frac-model` bar (one bar) → `stack`, `wholes`, `brackets`, `labels: 'unit'`, `whole: '100%'` |
| R55 | Fraction number line (past 1: 0–3, 0–4; fraction blocks sitting on the line) | B4 S4–S5, S7, S16; B6 S1–S2 | Y3 | PARTIAL | `fraction_nl_drag`, `mixed_nl_drag` (`ticks`) → `blocks: true` |
| R56 | Two stacked fraction lines (equivalence; subtraction hops) | B4 S1–S2, S14 | Y3 | PARTIAL | `frac-model` line → `stack: 2` |
| R57 | Fraction of a set (pictured objects ringed) | B6 S4 | Y3 | PARTIAL | `fractions:fraction_of_set` (legacy) → `template:arrays` rings |
| R58 | Data / listing table with blanks (populations, masses, conversions, cube table, translation table, temperatures) | B1 S11, S14; B2 S4; B3 S7; B9 S1, S3; B11 S4; B13 S1, S5; B14 S5 | Y3 | GAP | `template:data-table` |
| R59 | Sort table (2–5 labelled columns) | B3 S5; B6 S4; B10 S2; B12 S6, S8 | Y3 | PARTIAL | `hands-sort` page tag; kit `template:sort-table` GAP |
| R60 | Analogue clock (minute-hand turn in degrees) | B10 S1 | Y3 | MATCH | `template:clock` |
| R64 | Time line with hops (to the hour, hours, minutes) | B14 S6 | Y3 | MATCH | `template:timeline` (`elapsed_find_duration`) |
| R69 | Price tags / price boards on pictured items | B2 S4, S6; B5 S6; B12 S1, S4 | Y3 | PARTIAL | money word problems (`template:word-work`), `money-columns` (`currency`) |
| R73 | Two-way table with totals (read, complete, build from sentences) | B9 S4 | Y3 | GAP | `graphs:two_way_table` on R58 |
| R74 | Angles with arcs / right-angle marks (classify, estimate, in a polygon) | B10 S2–S3, S8 | Y3 | PARTIAL | `angles_lines:identify_angles`, `measure_angles` (MC estimate; legacy) → kit `template:angle` |
| R76 | Lines of symmetry on shapes (on squared paper; vertical / horizontal / diagonal) | B11 S5 | Y3 | PARTIAL | `angles_lines:symmetry`, `place_symmetry_lines` (legacy) |
| R78 | 2-D polygons with equal-side hatch marks; regular / irregular; polygon or not | B10 S9 | Y3 | MATCH / PARTIAL | `shapes_classify:classify_quads`, `shapes_early:shape_attributes` → hatch marks, `task: 'regular'` |
| R79 | 3-D solids (name, match, faces / edges / vertices) | B10 S10 | Y3 | MATCH | `shapes_early:name_3d_shapes`, `count_edges_faces_vertices` |
| R80 | Turns: compass rose (8 points), a labelled-direction star, a figure turning | B10 S1, S6–S7 | Y3, Y1 R64 | GAP | `angles_lines:turns` |
| R81 | Digit / number cards into box templates (□□□□ × □□; □.□□; □ < □) | B1 S3, S10; B4 S7, S16; B5 S5–S6; B7 S3; B12 S3–S4, S6 | Y3 | PARTIAL | `template:cloze-bank`; box template GAP |
| R82 | Grouped-object pictures for word problems (bags, boxes, packs) | B2 S6; B5 S11; B6 S4 | Y3 | PARTIAL | `template:wordpic` / `pictures` option |
| R87 | Covered / hidden part (splat over counters, digits, a hundred square) | B1 S8; B5 S5; B7 S12 | Y2, Y1 R32 | GAP | `cover` mark |
| R92 | Shape as the unknown (in an inequality) | B2 S8 | Y2, Y1 R39 | GAP | shape-symbol unknowns |
| R96 | Reflection / symmetry completion on squared paper (mirror line; shade squares) | B11 S5–S6 | Y2 | GAP | `template:symmetry-grid` → merged with N5-18 |
| R98 | Thermometer (vertical and **horizontal**; below zero) | B13 S1–S2, S4 | Y2 | PARTIAL | `measurement:temperature` (`createThermometerSVG`, vertical −10…40) → `orientation`, `min/max/every` |
| Y1 R48 | "Which pictures show n?" / odd one out across representations | B1 S2; B7 S2–S3 | Y1 | GAP | `task: 'which'` across panes |
| Y1 R79 | True / false and always / sometimes / never statement cards | every block (reasoning pages) | Y1 | MATCH | reasoning page role (true / false) |
| **N5-01** | **Roman numerals** (symbol → value table, partition into Roman parts, match cards, dates, sequences, calculations) | B1 S1, S8, S10 | **NEW** | GAP | `placevalue:roman_numerals` (+ Y3 R61 Roman clock numerals reuse the converter) |
| **N5-02** | **Period-grouped PV chart** (Thousands \| Ones, H T O under each; commas in threes) | B1 S4–S6, S11 | **NEW** | GAP | `heads: 'periods'` on the pv chart, `pv_digit_drag`, `number_word_names` support |
| **N5-03** | **Decimal PV chart** (O · Tth \| Hth \| Thth, the point on the column line) with **0.1 / 0.01 / 0.001 counters** (also fraction-labelled 1/10 …) and decimal digit charts | B7 S1, S5–S9, S14; B12 S1, S4–S8, S10–S12 | **NEW** | GAP | extend `pv.js` places below the ones (`LETTER` 0.1 → Tth …), `disk: 'fraction'`; decimal bands on `place_value_disks` / `pv_disks_build` / `value` / `expand` |
| **N5-04** | **Gattegno chart** (rows 1–9 × powers of ten, incl. decimal rows; ring cells; arrows up / down a row) | B1 S6–S7; B3 S8; B12 S10–S12 | **NEW** | GAP | `template:gattegno` (`rows`, `task: 'read' \| 'ring' \| 'shift'`) as a support on `place_value_10x`, `more_less_*` |
| **N5-05** | **Hundred square as one whole** (columns = tenths, cells = hundredths; shade / read; two squares for > 1; percent) | B7 S1, S3–S4, S12–S15; B12 S1–S2 | **NEW** | PARTIAL | `conversions:percent_visual`, `fraction_operations:frac_10_100` (legacy) → kit `template:hundred-square` (`labels: 'decimal' \| 'fraction' \| 'percent'`) |
| **N5-06** | **Thousand square** (1,000-cell grid) | B7 S5 | **NEW** | GAP | `template:hundred-square` `grid: 1000` |
| **N5-07** | **Percentage bar** (a "100 %" bar in tenths / quarters / fifths, % in each part; 10 % split into 1 %) | B7 S12–S15 | **NEW** | GAP | `frac-model` bar `whole: '100%'` + `conversions` skills |
| **N5-08** | **Equivalence multiplier arrows** (× n / ÷ n arcs over the numerators and under the denominators) | B4 S1–S3 | **NEW** | GAP | mark pane `scale-arrows` (support on `equivalent`, `equiv_frac_nv`, `simplify`) |
| **N5-09** | **Long multiplication** with partial-product rows, placeholder zero and "(__ × __)" row labels | B5 S3–S5 | **NEW** | MATCH | `template:long-multiplication` (`multiplication:long_multiplication_4x2`, tiles 32 / 42: partial rows, row notes, placeholder 0, carries); 2 × 2 on `multiplication:multiply` `tiles: 22` |
| **N5-10** | **Short division (bus stop)** with carried digits and a remainder "r" | B5 S7–S11 | **NEW** | PARTIAL | `template:short-division` (`multiplication:long_multiplication_4x2` forms 1: the bus stop, quotient boxes, exchange boxes before each digit; exact division) → a remainder "r" box still to add |
| **N5-11** | **Number pyramid** (each brick the sum of the two below; whole numbers or decimals) | B2 S5; B12 S5 | **NEW** | GAP | `template:pyramid` (`rows` 3–5, blanks anywhere) |
| **N5-12** | **Factor-pair list / two-row factor table** (systematic, crossed-out non-factors) | B3 S3, S6 | **NEW** | PARTIAL | `number_theory:factor_tchart_*`, `factor_links_*` (legacy drag, B&W) → kit template |
| **N5-13** | **Isometric cube solids** (linking-cube shapes, cube of cubes, cuboids in layers) and **isometric dot paper** | B3 S7; B10 S10; B15 S1–S3 | **NEW** | PARTIAL | `area_perimeter:volume`, `volume_composite` (legacy boxes) → `template:cube-solid` + `template:iso-grid` |
| **N5-14** | **Protractor** (semicircle, inner / outer scales; full-circle 360°) over an angle | B10 S4–S6 | **NEW** | GAP | `template:protractor` + `angles_lines:read_protractor` |
| **N5-15** | **Angles at a point / on a straight line** (sectors with arcs, known angles, letter unknowns; crossing lines) | B10 S6–S8 | **NEW** | PARTIAL | `angles_lines:additive_angles` (legacy, 2 parts, text) → kit `template:angle-sum` (`around: 180 \| 360`, 2–6 parts) |
| **N5-16** | **First-quadrant coordinate grid** (read / plot / join; axes without grid lines) | B11 S1–S2, S4 | **NEW** | PARTIAL | `coordinates:coordinate_q1`, `coordinate_graph`, `coord_polygon` (legacy) → kit `template:coord-grid` |
| **N5-17** | **Translation on a grid** (draw the image; "__ right / left, __ up / down" frame; coordinate table) | B11 S3–S4 | **NEW** | PARTIAL | `coordinates:geo_translate` (legacy MC) → `coord-grid` `task: 'translate'` |
| **N5-18** | **Reflection in a vertical / horizontal mirror line** (squared or coordinate grid; image coordinates) | B11 S6 | **NEW** | PARTIAL | `coordinates:geo_reflect` (legacy MC) → `coord-grid` `task: 'reflect'` (shared with R96) |
| **N5-19** | **Line graph** (plot from a table, read between points, choose the axis scale; sketch graphs to stories) | B9 S1–S2 | **NEW** | GAP | `graphs:line_graph` |
| **N5-20** | **Timetables** (train / bus stops × times; school timetable grid; TV-guide grid and block timeline) | B9 S5; B14 S6 | **NEW** | GAP | `template:data-table` `kind: 'timetable'` + `measurement:timetable` |
| **N5-21** | **Number line through zero**, horizontal and **vertical** (lift panel, sea-level scale) | B13 S1–S5 | **NEW** | MATCH / PARTIAL | horizontal MATCH `integers:number_line_int` (value-line), `integer_nl_drag`; vertical → `orientation: 'vertical'` |
| **N5-22** | **Conversion ladder** (unit boxes mm → cm → m with ÷ arrows above, × arrows below) | B14 S1, S3, S5 | **NEW** | GAP | structural scaffold pane `ladder` on `length_metric`, `unit_conversions` |
| **N5-23** | **Estimate area of an irregular shape on a cm grid** (full / more-than-half squares) | B8 S6 | **NEW** | GAP | `area_perimeter:estimate_area` on `pane:gridpaper` |
| **N5-24** | **Compound rectilinear area split into A / B** (dashed partition; subtract the missing corner) | B8 S5; B10 S8 | **NEW** | MATCH / PARTIAL | `area_perimeter:area_polygon_decompose`, `composite_shapes`; `method: 'subtract'` PARTIAL |
| **N5-25** | **One number, four ways** grid (counters \| part-whole \| bar \| PV chart) | B1 S3 | **NEW** | GAP | low; a reasoning / lesson-page frame over existing panes |
| **N5-26** | **Two worked methods side by side** (Method 1 \| Method 2; which is more efficient / correct) | B3 S10; B4 S16–S17; B5 S3, S6, S10; B8 S1 | **NEW** | PARTIAL | error-analysis / reasoning page roles; a `method-pair` frame |
| **N5-27** | **Partition flow for mixed numbers** (split into whole + fraction, operate, recombine) | B4 S12–S13; B6 S3 | **NEW** | GAP | low; bond `values: 'fraction'` (Y3 R14) chained |
| **N5-28** | **Counter-pattern terms** (1st, 2nd, 3rd term as growing counter columns) | B12 S9 | **NEW** | GAP | low; `count-row` with pictured terms |
| **N5-29** | **Square and cube notation** (n², n³; tables of squares / cubes) | B3 S6–S7, S8 | **NEW** | GAP | `number_theory:square_numbers`, `cube_numbers` (array / cube picture + notation) |
| **N5-30** | **Rounding line** (two bounding multiples, halfway, bounds circled, hops to each bound; to 1,000,000 and to 1 dp) | B1 S12–S14; B2 S4; B7 S10–S11 | **NEW** | MATCH / PARTIAL | `number_sense:rounding_visual`, `nearest_*`, `round_nl_*` (`pane:round-line`, `round-chart`, `round-pv`, integers only) → decimals, `hops: 'bounds'` |

**Counts (92 representations: 62 shared with Years 1–3 + 30 new):** MATCH 7 · MATCH / PARTIAL 12 ·
PARTIAL 36 · GAP 37. (Shared rows: 56 from Year 3, 4 first catalogued in Year 2, 2 in Year 1.)
Of the PARTIAL rows, about 8 close by **wiring existing panes or widening bands** (R03, R04 6-digit bands,
R17, R20, R25, R30 bands, R06, N5-30), 7 by **`frac-model` gaining modes** (`stack`, `wholes`, `cross`,
`split`, `blocks`, `whole: '100%'` — R32, R53–R56, N5-07), and 12 are **legacy visuals** awaiting the
kit / B&W migration (R16, R46, R50, R51, R57, R74, R76, R98, N5-05, N5-12, N5-13, N5-15–N5-18).

---

## Representations NEW relative to Years 1–3

Years 1 (`year-1.md`), 2 (`year-2.md`, R85–R101) and 3 (`year-3.md`, R01–R84) have no row for these 30
Year 5 representations:

- **Place value to a million and beyond the point:** N5-01 Roman numerals; N5-02 period-grouped
  (Thousands \| Ones) chart; N5-03 decimal PV chart with 0.1 / 0.01 / 0.001 counters; N5-04 Gattegno chart;
  N5-30 rounding line (Year 3 has no rounding step; our rounding panes exist but are integer-only).
- **Decimals and percentages:** N5-05 hundred square as one whole; N5-06 thousand square; N5-07 percentage
  bar.
- **Written methods:** N5-09 long multiplication with partial rows; N5-10 short (bus-stop) division;
  N5-26 two worked methods side by side.
- **Number puzzles and notation:** N5-08 equivalence multiplier arrows; N5-11 number pyramid; N5-12
  factor-pair table; N5-27 mixed-number partition flow; N5-28 counter-pattern terms; N5-29 square / cube
  notation; N5-25 one-number-four-ways grid.
- **Geometry:** N5-13 isometric cube solids and dot paper; N5-14 protractor; N5-15 angles at a point / on
  a line; N5-16 coordinate grid; N5-17 translation; N5-18 reflection in a mirror line.
- **Measures and data:** N5-19 line graph; N5-20 timetables; N5-21 vertical number line through zero;
  N5-22 conversion ladder; N5-23 estimating irregular area; N5-24 compound area split into rectangles.

(Rows with an earlier id reuse that id's proposal — e.g. R48 double number line serves both the Year 3
measures and the Year 5 fraction / decimal / percent lines; R96 symmetry completion and N5-18 reflection
should be one `coord-grid` / `symmetry-grid` template — so one build closes several years.)

**Suggested build order (by Year 5 reach × effort):**
1. **Decimal place value in the pv drawing** (N5-03: places 0.1 / 0.01 / 0.001, decimal counters, the point on
   the column line) **plus plain dots** (R05) — unlocks B7 S1, S5–S9 and B12 S1, S4–S12 (≈ 17 steps) and
   every chart row of B1. Widen the whole-number bands to 99,999 / 999,999 on `place_value_disks`,
   `pv_disks_build`, `more_less_100` (steps 10,000 / 100,000), `add_10k_*` / `sub_10k_*` (R03, R04, R30).
2. **`pane:pv-exchange`** (R31: the chart beside + − × ÷ with exchange rings, repeated rows and grouping
   rings) and the **point track in `stack`** (`dp: 1–3`, lifted from `money-columns`) — B2 S2–S3, B5 S1,
   S7–S11, B12 S4–S8 (≈ 14 steps).
3. **`frac-model` modes** — `stack`, `wholes` (mixed-number bars and bars past one), `cross`, `split`,
   `brackets`, `blocks` on the line, `whole: '100%'` — plus `fill` / `task: 'whole'` for R27 — the whole of
   B4 and B6 (24 steps) and the percentage bars of B7.
4. **Written methods:** `stack` `partials` (N5-09, tiles 32 / 42 / 41) and `division` `form: 'short'`
   (N5-10) — B5 S1, S3–S5, S7–S11 (9 steps).
5. **Hundred square as a whole** (N5-05, N5-06) and the **double / triple number line** (R48,
   `template:double-scale`) — B7 S2–S5, S12–S15, B12 S1–S2, B14 S1–S2, S5 (≈ 14 steps; R48 is shared with
   Year 3 measures).
6. **Geometry family:** `template:protractor` (N5-14), `angle-sum` (N5-15), `coord-grid` with read / plot /
   translate / reflect (N5-16–N5-18, merging Y2 R96), `angles_lines:turns` (R80) — all of B10 and B11
   (16 steps).
7. **Data:** `template:data-table` (R58) with `kind: 'timetable'` (N5-20), two-way tables (R73), and
   `graphs:line_graph` (N5-19) — B9 (5 steps) and B14 S6, plus the many small tables elsewhere.
8. **Gattegno chart** (N5-04) and **Roman numerals** (N5-01) — B1 S1, S6–S8, S10; B3 S8; B12 S10–S12.
9. **Negative numbers and measures:** `orientation: 'vertical'` on the value-line and a horizontal
   thermometer (N5-21, R98), conversion ladder and bars (N5-22, R29), `cube-solid` + isometric paper
   (N5-13), `estimate_area` (N5-23) — B13, B14, B15, B8 S6.
10. Smaller items: Venn / Carroll (R43, R44), number pyramid (N5-11), multiplier arrows (N5-08), square /
    cube numbers (N5-29), rounding panes for decimals (N5-30), period heads (N5-02), tenths ten frame
    (R10), bead string (R07), cover mark (R87), method-pair frame (N5-26).

---

## Method notes

Same method as the pilot (`year-3.md` § Method notes), with these Year 5 differences:

- **File ids straight from `data/curriculum/wrm-steps.json`** (branch `claude/sweet-newton-c8wrv1`): each
  step's `drive.lesson` and `drive.guide`. No folder listing was needed.
- **Downloads by `curl`, not the Drive tool.** The files are shared "anyone with the link", so
  `curl -sL "https://drive.google.com/uc?export=download&id=<id>" -o <stepId>.pdf` fetches each PDF directly
  (8 in parallel, one retry loop, a `%PDF` magic-byte check). Nothing passes through the conversation or the
  shared `tool-results` folder, so **the same-millisecond collision and the cross-lane decode theft of the
  earlier runs cannot happen**; 136 guides and 136 lessons arrived with no loss and no re-download. Scripts:
  `scratchpad/wrm/year-5/dl.py` (download by kind and block), `guides.py` (key-model / avoid extraction for
  all guides in one pass, ≈ 38 KB of text for 136 steps), `tile.py` (pages 2–3, 52 dpi, three steps per
  2 × 3 sheet). Recommended for the remaining years.
- **Cost.** 136 steps: one guide pass, 15 block downloads (lesson PDFs 1–3 MB), 46 contact sheets read.
  PDFs were deleted after each block; disk stayed under 50 MB.
- **What the WRM pages lean on in Year 5** (for the build lanes): the **PV chart with counters is drawn
  beside every written method** (add, subtract, short multiply, short divide, decimal add / subtract), and
  the guides forbid shortcuts it replaces ("add a zero", "remove a zero", "round up / down"); fractions
  are **always equal-length stacked bars** before any rule; decimals and percentages are anchored on the
  **hundred square as one whole** and the **double number line**; the reasoning pages are dominated by
  true / false, always / sometimes / never, "spot the mistake" and "two methods" items — so Year 5's error
  analysis and reasoning page roles need the method-pair frame (N5-26) as much as new drawings.
- **Mapping notes.** Year 5 = US Grade 4, but several Year 5 steps sit at Grade 5 in CCSS (decimals to
  thousandths, powers of ten, volume by cubes, first-quadrant coordinates) or beyond CCSS (Roman numerals,
  metric ↔ imperial approximations, negative numbers, translation / reflection) — the representation rows
  are catalogued regardless; `wrm-steps.json` carries the flags. Money in £ / p maps to the `currency`
  option; UK spellings (litre, metre) are content, not a gap.
